\section{Campaign details}
\label{app:campaign}

This appendix lists the checkpoints and their consumed loci, the acceptance decisions, the coverage of the campaign, and the per-concept results of every completed block.

\subsection{Grid and consumed loci}
\label{app:cf-grid}

\input{tables/table_cf_models}
\input{tables/table_cf_gates}

For Llama~3.2 Vision the projector also reads five intermediate local-layer outputs that bypass the final global block; for LLaVA the final CLIP block is computed but not consumed, so the consumed block is the penultimate one. Gate (C) verifies for every checkpoint that the write reaches the consumer and the answer logits and changes nothing outside the consumed tokens.

\subsection{Acceptance decisions}
\label{app:cf-gates}

All 57 completed blocks pass checks (A), (B), (C), and (G) with zero determinism error, zero change at $\alpha=0$, and fp32-versus-model logit differences below 0.13. Check (D) exceeds the 0.25-logit tolerance for the Qwen3-VL, Gemma~3, MedGemma, InternVL, and Llama families (up to 1.84 logits for Gemma~3 12B); these are recorded as declared deviations and neutralised by the fixed batch composition. Check (E) fails the A/B templates for LLaVA-1.5-7B and LLaVA-Med, the B-mapped templates for Qwen2.5-VL-3B and LLaVA-1.5-13B, and the yes/no and A-mapped templates for Llama~3.2 Vision 11B on all datasets; the affected cells are recorded as ineligible. Two checkpoints answer ``yes'' to nearly every chest question (Gemma~3 4B: clean $P(\mathrm{yes})>0.99$ on 94--97\% of rows), which the answerability grade records; three (LLaVA-1.5 7B/13B, LLaVA-Med) answer at chance.

\subsection{Coverage and cost}
\label{app:cf-coverage}

Nineteen checkpoints are complete on all three datasets with every module (the write matrix, calibration, the five additional templates, the dose sweep, the two refits, and the connector locus on NIH and COCO; the write matrix and calibration on CheXpert); Llama~3.2 Vision 11B is complete with its yes/no modules ineligible; Qwen2.5-VL-72B is complete on NIH; the 72B COCO/CheXpert blocks and the 90B are deferred, and InternVL3.5-38B is in progress at submission. The campaign consumed about 2,800 A100-hours; per-checkpoint costs are in Table~\ref{tab:cf-models}.

\subsection{Seed replication details}
\label{app:cf-seed}

Qwen2.5-VL-7B on NIH: Effusion $W=0.190$, random p95 $=0.137$, $|\mathrm{sham}|=0.017$, rank 2 of 120, $O=-0.071$ $[-0.080,-0.062]$ with Nodule the maximiser; all six diagonals negative (Atelectasis $-0.226$, Pneumothorax $-0.102$, Cardiomegaly $-0.219$, Mass $-0.169$, Nodule $-0.072$). Mass wording: the mapping contrast IA$-$IB and the wording contrast IY$-$WY are reported for every block in the campaign tables; for the seed model Mass is owned under the \emph{show} A/B templates only, as in the original confirmation. Effusion does not transfer to CheXpert for the seed model ($W=0.08$, below the random p95).

\subsection{Per-concept results}
\label{app:cf-tables}

\input{tables/table_cf_nih}
\input{tables/table_cf_chexpert}
\input{tables/table_cf_coco}

\subsection{Controls}
\label{app:cf-controls-sec}

\input{tables/table_cf_controls}

\begin{figure}[h]
    \centering
    \includegraphics[width=\textwidth]{figures/fig6_controls.pdf}
    \caption{\textbf{Controls per block.} (a) Connector-locus against primary-locus ownership. (b) Refit standard deviation of ownership over three fit seeds. (c) Label-shift gap. (d) Wording and mapping contrasts of ownership.}
    \label{fig:controls}
\end{figure}

\subsection{Write matrices and controls for every block}
\label{app:cf-figures}

\begin{figure}[h]
    \centering
    \includegraphics[width=\textwidth]{figures/figA7_write_matrices_nih.pdf}
    \caption{Write matrices $W_{q,d}$ of every checkpoint on NIH ChestX-ray14; owned diagonals are outlined.}
    \label{fig:cf-w-nih}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=\textwidth]{figures/figA8_write_matrices_chexpert.pdf}
    \caption{Write matrices of every checkpoint on CheXpert Plus.}
    \label{fig:cf-w-chexpert}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=\textwidth]{figures/figA9_write_matrices_coco.pdf}
    \caption{Write matrices of every checkpoint on COCO.}
    \label{fig:cf-w-coco}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=\textwidth]{figures/figA10_controls_all.pdf}
    \caption{Connector-locus ownership, refit standard deviation, and label-shift gap for every block with the full module set.}
    \label{fig:cf-controls-all}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=\textwidth]{figures/figA11_prompt_dependence.pdf}
    \caption{Wording and mapping contrasts of ownership for every block with the template module.}
    \label{fig:cf-prompt}
\end{figure}


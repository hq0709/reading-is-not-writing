# GPT-6-Astra paragraph pass 2026-09-15 13:29

## 4_setup paragraph 4 — ACCEPTED

**before**

\paragraph{Scale.}
A block pairs one checkpoint with one dataset. Each block scores the $6\times6$ write matrix, the sham, and 119 random directions on 600 test rows (457{,}200 outcomes) and, on NIH and COCO, the calibration, dose, refit, connector-locus, and template modules (CheXpert blocks add calibration). The campaign comprises \cfBlocks{} blocks, \cfCells{} scored concept cells, about 30 million scored answers, and roughly 3{,}000 A100-hours. \cfCheckpointsCompleteWord{} checkpoints are complete on all three datasets, Llama~3.2~Vision 11B graded on its eligible B-positive template. We run Qwen2.5-VL-72B on NIH only. The largest checkpoint run is 72B (Appendix~\ref{app:campaign}; label handling, registration, and the released artifact are in Appendix~\ref{app:cf-labels}).

**after**

\paragraph{Scale.}
A block pairs one checkpoint with one dataset. Each block scores the $6\times6$ write matrix, the sham, and 119 random directions on 600 test rows (457{,}200 outcomes). NIH and COCO blocks also run the calibration, dose, refit, connector-locus, and template modules; CheXpert blocks add calibration. The campaign comprises \cfBlocks{} blocks, \cfCells{} scored concept cells, about 30 million scored answers, and roughly 3{,}000 A100-hours. \cfCheckpointsCompleteWord{} checkpoints are complete on all three datasets. Llama~3.2~Vision 11B is graded on its eligible B-positive template. We run Qwen2.5-VL-72B on NIH only. The largest checkpoint run is 72B (Appendix~\ref{app:campaign}; label handling, registration, and the released artifact are in Appendix~\ref{app:cf-labels}).

## app_campaign paragraph 13 — ACCEPTED

**before**

\cfCheckpointsCompleteWord{} checkpoints are complete on all three datasets with every module (Table~\ref{tab:cf-coverage}): the write matrix, calibration, the five additional templates, the dose sweep, the two refits, and the connector locus on NIH and COCO; the write matrix and calibration on CheXpert. Llama~3.2 Vision 11B is graded on its eligible B-positive template IB. Qwen2.5-VL-72B is complete on NIH. The 72B COCO/CheXpert blocks and Llama~3.2 Vision 90B were not run (compute budget). InternVL3.5-38B is complete on all three datasets. The campaign consumed about 3,000 A100-hours; a chest block of a 7B checkpoint takes about ten GPU-hours.

**after**

\cfCheckpointsCompleteWord{} checkpoints are complete on all three datasets (Table~\ref{tab:cf-coverage}). Coverage includes every module on NIH and COCO---the write matrix, calibration, the five additional templates, the dose sweep, the two refits, and the connector locus---and the write matrix and calibration on CheXpert. Llama~3.2 Vision 11B is graded on its eligible B-positive template IB. Qwen2.5-VL-72B is complete on NIH. The 72B COCO/CheXpert blocks and Llama~3.2 Vision 90B were not run (compute budget). InternVL3.5-38B is complete on all three datasets. The campaign consumed about 3,000 A100-hours. A chest block of a 7B checkpoint takes about ten GPU-hours.


{"sent": 2, "accepted": 2, "rejected": 0}

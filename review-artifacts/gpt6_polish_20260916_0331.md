# GPT-6-Astra paragraph pass 2026-09-16 03:31

## 5_results paragraph 21 — ACCEPTED

**before**

\paragraph{Alternative direction constructions.} The deficit is not specific to the logistic normal. Four alternative directions use the same projected, train-scaled features and write protocol (Table~\ref{tab:cf-altdir}): the difference of class means, the Haufe pattern $\Sigma\boldsymbol\beta_c$ \citep{haufe2014interpretation}, the normal orthogonalised against the other five normals, and the normal refitted on features residualised against the other five labels. Across the \cfAltdirBlocks{} blocks scored so far (\cfAltdirBlocksPerDs{} per dataset), each construction owns \cfAltdirChestPctMin{}--\cfAltdirChestPctMax\% of chest cells and \cfAltdirCocoPctMin{}--\cfAltdirCocoPctMax\% of COCO cells. The difference-of-means and pattern directions have cosines of $0.1$--$0.4$ with the logistic normal. Each owns more NIH cells and fewer COCO cells than the logistic normal. Lifted as displacements, they respectively own \cfAltdirdChestDom{} and \cfAltdirdChestPattern{} of \cfAltdirdChestN{} chest cells and \cfAltdirdCocoDom{} and \cfAltdirdCocoPattern{} of \cfAltdirdCocoN{} COCO cells (Table~\ref{tab:cf-altdird}). Every construction preserves the chest-to-COCO gap. At least one construction owns \cfAltdirNihAny{} of \cfAltdirNihN{} cells on NIH and \cfAltdirChexAny{} of \cfAltdirChexN{} on CheXpert, compared with \cfAltdirCocoAny{} of \cfAltdirCocoN{} on COCO. Extending the clinical family to every additional dataset label with enough support retains 5 of the 6 owned cells in the 8 scored blocks (Table~\ref{tab:cf-extcomp}). Projection, sex, and age join one nine-direction family. They own \cfAttrChestAttrOwned{} of \cfAttrChestAttrN{} cells, findings \cfAttrChestClinOwned{} of \cfAttrChestClinN{}, \cfAttrNihAttrOwned{}/\cfAttrNihAttrN{} against \cfAttrNihClinOwned{}/\cfAttrNihClinN{} on NIH (Table~\ref{tab:cf-attr}). We weight writes along the logistic directions by the softmax of each token's probe score or restrict them to the top quarter of tokens at four times the weight (twice the Frobenius norm of the uniform write). These writes own 0 of 6 chest concepts and 6 of 6 COCO objects in Qwen2.5-VL-7B and Qwen3-VL-8B (Table~\ref{tab:cf-tokenw}). Concentrating the write where the probe reads does not create a clinical handle.

**after**

\paragraph{Alternative direction constructions.} The deficit is not specific to the logistic normal. Four alternative directions use the same projected, train-scaled features and write protocol (Table~\ref{tab:cf-altdir}): the difference of class means, the Haufe pattern $\Sigma\boldsymbol\beta_c$ \citep{haufe2014interpretation}, the normal orthogonalised against the other five normals, and the normal refitted on features residualised against the other five labels. Across the \cfAltdirBlocks{} scored blocks (\cfAltdirBlocksPerDs{} per dataset), each construction owns \cfAltdirChestPctMin{}--\cfAltdirChestPctMax\% of chest cells and \cfAltdirCocoPctMin{}--\cfAltdirCocoPctMax\% of COCO cells. The difference-of-means and pattern directions have cosines of $0.1$--$0.4$ with the logistic normal. Each owns more NIH cells and fewer COCO cells than the logistic normal. Lifted as displacements, these directions respectively own \cfAltdirdChestDom{} and \cfAltdirdChestPattern{} of \cfAltdirdChestN{} chest cells and \cfAltdirdCocoDom{} and \cfAltdirdCocoPattern{} of \cfAltdirdCocoN{} COCO cells (Table~\ref{tab:cf-altdird}). Every construction preserves the chest-to-COCO gap. At least one construction owns \cfAltdirNihAny{} of \cfAltdirNihN{} cells on NIH and \cfAltdirChexAny{} of \cfAltdirChexN{} on CheXpert, compared with \cfAltdirCocoAny{} of \cfAltdirCocoN{} on COCO. Extending the clinical family to every additional dataset label with enough support retains 5 of the 6 owned cells in the 8 scored blocks (Table~\ref{tab:cf-extcomp}). Projection, sex, and age join the findings in one nine-direction family. The attributes own \cfAttrChestAttrOwned{} of \cfAttrChestAttrN{} cells; findings own \cfAttrChestClinOwned{} of \cfAttrChestClinN{}. On NIH, ownership is \cfAttrNihAttrOwned{}/\cfAttrNihAttrN{} for attributes and \cfAttrNihClinOwned{}/\cfAttrNihClinN{} for findings (Table~\ref{tab:cf-attr}). We weight writes along the logistic directions by the softmax of token probe scores or restrict writes to the top quarter of tokens at four times the weight (twice the Frobenius norm of the uniform write). These writes own 0 of 6 chest concepts and 6 of 6 COCO objects in Qwen2.5-VL-7B and Qwen3-VL-8B (Table~\ref{tab:cf-tokenw}). Concentrating the write where the probe reads does not create a clinical handle.

## app_campaign paragraph 22 — ACCEPTED

**before**

Every alternative direction is a vector $\boldsymbol u$ in the projected, train-scaled space. We lift it to the consumed space exactly as the logistic normal, by normalising $\hat\wvec=R(\boldsymbol u\oslash\max(\boldsymbol s,10^{-8}))$ (Equation~\ref{eq:lift}). Here $x=R^{\top}\hvec$, where $R$ is a fixed Gaussian matrix with near-orthonormal columns. A displacement $R\boldsymbol\delta$ of the consumed representation therefore produces the displacement $R^{\top}R\boldsymbol\delta\approx\boldsymbol\delta$ of the projected features. The same lift carries classifier normals, class-mean displacements, and patterns alike. We compute the difference-of-means and pattern vectors in the projected space and lift them once. The token-weighted variants scale the per-token dose $\alpha\lVert\hvec_{it}\rVert$ by a weight $w_t$ with mean one over the consumed tokens. The softmax variant preserves the L1 dose of the uniform write. The top-quarter variant ($w_t=4$ on the 25\% highest-scoring tokens) has twice the uniform write's Frobenius norm. The attribute directions take three labels read from each dataset's own metadata---the view field (anteroposterior or portable against posteroanterior), the recorded sex, and age at least 60---and are fitted on the same images, features, projection, and probe settings as the clinical probes, then lifted and written together with the six clinical directions as one nine-direction family. Attribute questions have no random family, so their steering reference is the sham alone. Their probes reach a median AUROC of \cfAttrAurocMin{}--\cfAttrAurocMax{} against the attribute label and \cfAttrAnswerAurocMin{}--\cfAttrAnswerAurocMax{} against the model's own clean answer, and the largest median $|\cos|$ between an attribute direction and a clinical normal is \cfAttrCosMax{}. Across the \cfAttrBlocks{} blocks with the module, the attributes are owned in \cfAttrChestAttrOwned{} of \cfAttrChestAttrN{} cells and the findings of the same family in \cfAttrChestClinOwned{} of \cfAttrChestClinN{}, with \cfAttrChexAttrOwned{}/\cfAttrChexAttrN{} against \cfAttrChexClinOwned{}/\cfAttrChexClinN{} on CheXpert and \cfAttrNihAttrOwned{}/\cfAttrNihAttrN{} against \cfAttrNihClinOwned{}/\cfAttrNihClinN{} on NIH.

**after**

Each alternative direction is a vector $\boldsymbol u$ in the projected, train-scaled space. We lift it to the consumed space by normalising $\hat\wvec=R(\boldsymbol u\oslash\max(\boldsymbol s,10^{-8}))$, exactly as for the logistic normal (Equation~\ref{eq:lift}). Here $x=R^{\top}\hvec$, where $R$ is a fixed Gaussian matrix with near-orthonormal columns. Displacing the consumed representation by $R\boldsymbol\delta$ therefore displaces the projected features by $R^{\top}R\boldsymbol\delta\approx\boldsymbol\delta$. The same lift applies to classifier normals, class-mean displacements, and patterns. We compute the difference-of-means and pattern vectors in the projected space and lift them once. The token-weighted variants multiply the per-token dose $\alpha\lVert\hvec_{it}\rVert$ by a weight $w_t$ whose mean over the consumed tokens is one. The softmax variant preserves the L1 dose of the uniform write. The top-quarter variant ($w_t=4$ on the 25\% highest-scoring tokens) has twice the uniform write's Frobenius norm. The attribute directions use three labels from each dataset's own metadata: the view field (anteroposterior or portable against posteroanterior), the recorded sex, and age at least 60. We fit these directions using the same images, features, projection, and probe settings as the clinical probes. We then lift and write them with the six clinical directions as one nine-direction family. Attribute questions have no random family, so the sham is their sole steering reference. Their probes reach a median AUROC of \cfAttrAurocMin{}--\cfAttrAurocMax{} against the attribute label and \cfAttrAnswerAurocMin{}--\cfAttrAnswerAurocMax{} against the model's own clean answer. The largest median $|\cos|$ between an attribute direction and a clinical normal is \cfAttrCosMax{}. Across the \cfAttrBlocks{} blocks with the module, the attributes are owned in \cfAttrChestAttrOwned{} of \cfAttrChestAttrN{} cells and the findings of the same family in \cfAttrChestClinOwned{} of \cfAttrChestClinN{}. The corresponding counts are \cfAttrChexAttrOwned{}/\cfAttrChexAttrN{} against \cfAttrChexClinOwned{}/\cfAttrChexClinN{} on CheXpert and \cfAttrNihAttrOwned{}/\cfAttrNihAttrN{} against \cfAttrNihClinOwned{}/\cfAttrNihClinN{} on NIH.


{"sent": 2, "accepted": 2, "rejected": 0}

# GPT-6-Astra paragraph pass 2026-09-15 04:52

## 5_results paragraph 1 — ACCEPTED

**before**

Table~\ref{tab:cf-main} reports scores for every checkpoint on every dataset. Figure~\ref{fig:overview} shows every concept cell across the three grades. Both chest datasets show the same pattern. Clinical concepts are readable in \cfChestReadable{} of \cfChestReadableN{} cells (\cfChestReadablePct\%): probe selectivity reaches $0.1$--$0.3$ above matched random-label controls for most findings in most families. The models also answer: \cfChestAnswerable{} of the \cfChestAnswerableN{} cells with a scored write matrix (\cfChestAnswerablePct\%) have a clean-answer AUROC with a lower bound above chance. Among these answerable cells, \cfReadAns{} are also readable. Yet only \cfChestOwned{} cells (\cfChestOwnedPct\%) are owned. In \cfChestCompetitor{} cells (\cfChestCompetitorPct\%), the verdict is \emph{stronger competitor}---a direction fitted for another finding moves the concept's answer more than its own direction does. Readability and answerability do not suffice for writing: \cfReadAnsOwned{} of the \cfReadAns{} readable and answerable cells are owned (\cfReadAnsOwnedPct\%), compared with \cfRestOwned{} of the remaining \cfRestN{} (\cfRestOwnedPct\%). Ownership is the least common and least stable of the three grades on the chest sets. Applying an effect floor of $0.05$ to both $W_{q,q}$ and $O_q$ leaves \cfChestFloored{} of the \cfChestOwned{} owned chest cells (\cfChestFlooredPct\% of \cfChestAnswerableN{}), compared with \cfCocoFloored{} of the \cfCocoOwned{} owned COCO cells (\cfCocoFlooredPct\%). Refitting probes on resampled patients leaves \cfRefitGradeStableNih{} of the \cfRefitGradeOwnedNih{} owned NIH cells owned under both refits, compared with \cfRefitGradeStableCoco{} of \cfRefitGradeOwnedCoco{} on COCO (Table~\ref{tab:cf-refit}). Clinical writes are not selective on the chest sets. Of the answer change a clinical write produces across the six questions, \cfOwnShareNih\% lands on its own question on NIH and \cfOwnShareChex\% on CheXpert, compared with \cfOwnShareCoco\% on COCO (Figure~\ref{fig:cf-w-structure}). We call this pattern the attribution deficit. The model exposes the concept to a probe. The probe's direction is not a handle on the concept. Written back, it moves other findings' answers as much as its own.

**after**

Table~\ref{tab:cf-main} reports scores for every checkpoint on every dataset. Figure~\ref{fig:overview} shows every concept cell across the three grades. Both chest datasets show the same pattern. Clinical concepts are readable in \cfChestReadable{} of \cfChestReadableN{} cells (\cfChestReadablePct\%): probe selectivity reaches $0.1$--$0.3$ above matched random-label controls for most findings in most families. The models also answer: \cfChestAnswerable{} of the \cfChestAnswerableN{} cells with a scored write matrix (\cfChestAnswerablePct\%) have a clean-answer AUROC whose lower bound exceeds chance. Of these answerable cells, \cfReadAns{} are also readable. Yet only \cfChestOwned{} cells (\cfChestOwnedPct\%) are owned. In \cfChestCompetitor{} cells (\cfChestCompetitorPct\%), the verdict is \emph{stronger competitor}---a direction fitted for another finding moves the concept's answer more than its own direction does. Readability and answerability do not suffice for writing: \cfReadAnsOwned{} of the \cfReadAns{} readable and answerable cells are owned (\cfReadAnsOwnedPct\%), compared with \cfRestOwned{} of the remaining \cfRestN{} (\cfRestOwnedPct\%). Ownership is the least common and least stable of the three grades on the chest sets. Applying an effect floor of $0.05$ to both $W_{q,q}$ and $O_q$ leaves \cfChestFloored{} of the \cfChestOwned{} owned chest cells (\cfChestFlooredPct\% of \cfChestAnswerableN{}), compared with \cfCocoFloored{} of the \cfCocoOwned{} owned COCO cells (\cfCocoFlooredPct\%). Under both probe refits on resampled patients, \cfRefitGradeStableNih{} of the \cfRefitGradeOwnedNih{} owned NIH cells retain ownership, compared with \cfRefitGradeStableCoco{} of \cfRefitGradeOwnedCoco{} on COCO (Table~\ref{tab:cf-refit}). Clinical writes are not selective on the chest sets. Of the answer change a clinical write produces across the six questions, \cfOwnShareNih\% lands on its own question on NIH and \cfOwnShareChex\% on CheXpert, compared with \cfOwnShareCoco\% on COCO (Figure~\ref{fig:cf-w-structure}). We call this pattern the attribution deficit. The model exposes the concept to a probe. The probe's direction is not a handle on the concept. Written back, it moves other findings' answers as much as its own.

## 5_results paragraph 2 — ACCEPTED

**before**

Figure~\ref{fig:example} shows the pattern in individual images. On a chest radiograph, the Effusion and Nodule writes raise the same answers. The competing write raises the Effusion answer more than the Effusion write does. On a natural image, the dog write moves only the dog answer. The write matrix shows two parts of the deficit (Figure~\ref{fig:write-matrices}, Table~\ref{tab:cf-controls}). First, concept writes have little effect on their own answers on the chest sets. The median $W_{q,q}$ is $\cfMedianWqqNih$ on NIH and $\cfMedianWqqChex$ on CheXpert, compared with $\cfMedianWqqCoco$ on COCO. Writes meet the steering reference---beating the random family and the sham---in \cfChestRefMet{} of \cfChestAnswerableN{} chest cells, against \cfCocoRefMet{} of \cfCocoOwnedN{} COCO cells. Second, when a chest write moves an answer, competing directions share that effect. In \cfChestRefNotOwned{} of those \cfChestRefMet{} cells, a competing clinical direction moves the answer as much or more. Across the grid, the Atelectasis, Effusion, and Consolidation directions raise each other's answers by similar amounts. On COCO, the diagonal entry of $W$ dominates its row. On NIH and CheXpert, the row is flat or peaks off the diagonal. At every Qwen2.5-VL size, a competing direction---Cardiomegaly at 3B, Nodule at 7B and 32B, Atelectasis at 72B---raises the Effusion answer more than the Effusion direction does.

**after**

Figure~\ref{fig:example} shows the pattern in individual images. On a chest radiograph, the Effusion and Nodule writes raise the same answers. The competing write raises the Effusion answer more than the Effusion write does. On a natural image, the dog write moves only the dog answer. The write matrix reveals two components of the deficit (Figure~\ref{fig:write-matrices}, Table~\ref{tab:cf-controls}). First, concept writes have little effect on their own answers in the chest datasets. The median $W_{q,q}$ is $\cfMedianWqqNih$ on NIH and $\cfMedianWqqChex$ on CheXpert, compared with $\cfMedianWqqCoco$ on COCO. Writes meet the steering reference---beating the random family and the sham---in \cfChestRefMet{} of \cfChestAnswerableN{} chest cells, versus \cfCocoRefMet{} of \cfCocoOwnedN{} COCO cells. Second, competing directions share the effect when a chest write moves an answer. In \cfChestRefNotOwned{} of those \cfChestRefMet{} cells, a competing clinical direction moves the answer at least as much. Across the grid, the Atelectasis, Effusion, and Consolidation directions raise each other's answers by similar amounts. On COCO, the diagonal entry of $W$ dominates its row. On NIH and CheXpert, the row is flat or peaks off the diagonal. At every Qwen2.5-VL size, a competing direction---Cardiomegaly at 3B, Nodule at 7B and 32B, Atelectasis at 72B---raises the Effusion answer more than the Effusion direction does.

## 5_results paragraph 3 — ACCEPTED

**before**

The evaluation builds on a two-model NIH study of Qwen2.5-VL-7B and LLaVA-1.5-7B (Appendix~\ref{app:seed}). The central result reproduces on a 600-image cohort disjoint from every earlier cohort (Table~\ref{tab:cf-seed}). The Effusion write raises the Effusion answer by $0.190$. This exceeds the random 95th percentile ($0.137$) and sham ($0.017$). The effect ranks second among the 120 compared effects. The Nodule direction raises the Effusion answer by $0.262$. Effusion ownership is $O_{\mathrm{Effusion}}=-0.071$ (95\% CI $[-0.080,-0.062]$), matching the original $-0.057$ $[-0.069,-0.045]$. Prompt dependence also reproduces for Mass. Effusion and Cardiomegaly are the most readable chest concepts (mean selectivity $\cfSelEffusion$ and $\cfSelCardiomegaly$ over \cfChestProbeBlocks{} chest blocks). Effusion is owned in \cfEffusionOwnedBlocks{} of \cfChestBlocks{} chest blocks. Of these, \cfEffusionOwnedSmall{} have $O_{\mathrm{Effusion}}\leq0.03$.

**after**

The evaluation builds on a two-model NIH study of Qwen2.5-VL-7B and LLaVA-1.5-7B (Appendix~\ref{app:seed}). The central result reproduces in a 600-image cohort disjoint from every earlier cohort (Table~\ref{tab:cf-seed}). The Effusion write raises the Effusion answer by $0.190$. This increase exceeds the random 95th percentile ($0.137$) and sham ($0.017$). It ranks second among the 120 compared effects. Yet the Nodule direction raises the Effusion answer by $0.262$. Effusion ownership is $O_{\mathrm{Effusion}}=-0.071$ (95\% CI $[-0.080,-0.062]$), matching the original $-0.057$ $[-0.069,-0.045]$. Prompt dependence for Mass also reproduces. Effusion and Cardiomegaly are the most readable chest concepts (mean selectivity $\cfSelEffusion$ and $\cfSelCardiomegaly$ over \cfChestProbeBlocks{} chest blocks). Effusion is owned in \cfEffusionOwnedBlocks{} of \cfChestBlocks{} chest blocks. Of these owned blocks, \cfEffusionOwnedSmall{} have $O_{\mathrm{Effusion}}\leq0.03$.

## 5_results paragraph 4 — ACCEPTED

**before**

The same procedure yields the opposite result on COCO (Table~\ref{tab:cf-main}, right block). Objects are owned in \cfCocoOwned{} of \cfCocoOwnedN{} cells (\cfCocoOwnedPct\%), with ownership margins of $0.1$--$0.9$ in the Qwen, Lingshu, and Gemma families. \cfCocoRefMet{} cells meet the steering reference. Only \cfCocoCompetitor{} cells have a stronger competitor. \cfCocoBlocksFourPlus{} of \cfCocoBlocks{} checkpoints with a COCO block own at least four of the six objects. Dose response tracks the write. On COCO, median ownership across questions and blocks rises from $-0.03$ at $\alpha=-0.1$ to $+0.26$ at $\alpha=+0.25$ and remains positive at $\alpha=+0.5$ ($+0.18$). On NIH, it stays within $\pm0.1$ at every dose (Figure~\ref{fig:overview}c). The write, direction construction, reference family, and decision rules therefore work as intended. The deficit is specific to clinical concepts.

**after**

The same procedure produces the opposite result on COCO (Table~\ref{tab:cf-main}, right block). Objects are owned in \cfCocoOwned{} of \cfCocoOwnedN{} cells (\cfCocoOwnedPct\%), with ownership margins of $0.1$--$0.9$ in the Qwen, Lingshu, and Gemma families. \cfCocoRefMet{} cells meet the steering reference. Only \cfCocoCompetitor{} cells have a stronger competitor. Of the \cfCocoBlocks{} checkpoints with a COCO block, \cfCocoBlocksFourPlus{} own at least four of the six objects. Dose response tracks the write. Across questions and blocks on COCO, median ownership rises from $-0.03$ at $\alpha=-0.1$ to $+0.26$ at $\alpha=+0.25$ and remains positive at $\alpha=+0.5$ ($+0.18$). On NIH, median ownership stays within $\pm0.1$ at every dose (Figure~\ref{fig:overview}c). The write, direction construction, reference family, and decision rules therefore work as intended. The deficit is specific to clinical concepts.

## 5_results paragraph 5 — ACCEPTED

**before**

Gemma~3 shares one SigLIP tower across its 4B, 12B, and 27B checkpoints. MedGemma shares one MedSigLIP tower across 4B and 27B. LLaVA-1.5 shares one CLIP tower across 7B and 13B. The tower's final block is the primary intervention site. Within each family, pooled features, fitted probes, and write vectors are therefore identical across sizes (bit-identical for Gemma~3 4B and 12B and for LLaVA-1.5; equal within one bf16 rounding step for Gemma~3 27B and MedGemma 27B, direction cosines above $0.9987$). Readability is identical across sizes by construction. How the connector and language model read the same written representation determines every difference in Figure~\ref{fig:readers}. These differences are large relative to every noise source in the grid (Table~\ref{tab:cf-pairs}). With the identical write on the same 600 patients, Gemma~3 4B and 12B differ in $O_q$ by $\cfPairDeltaEffFourTwelve$ $[\cfPairDeltaEffFourTwelveLo,\cfPairDeltaEffFourTwelveHi]$ on NIH Effusion. The difference between 12B and 27B is $\cfPairDeltaEffTwelveTwentySeven$ $[\cfPairDeltaEffTwelveTwentySevenLo,\cfPairDeltaEffTwelveTwentySevenHi]$. The paired patient-bootstrap interval for $\Delta O_q$ excludes zero for five or six of the six concepts in \cfPairsFiveOrSix{} of the \cfPairsN{} shared-tower pair--dataset combinations. The median $|\Delta O_q|$ is \cfPairsRatioMin{}--\cfPairsRatioMax{} times the families' refit standard deviation (up to \cfPairsRatioSingleMax{} times for single concepts). Competing directions own the chest answers outright in 12B ($O_{\mathrm{Effusion}}=\cfOGemmaTwelveEff$ on both chest sets, $O_{\mathrm{Mass}}=\cfOGemmaTwelveMass$, $O_{\mathrm{Pneumothorax}}=\cfOGemmaTwelvePneu$). In 27B, no direction moves these answers ($\cfOGemmaTwentySevenEff$, $\cfOGemmaTwentySevenMass$, $\cfOGemmaTwentySevenPneu$). In 4B, the clean answer is yes on 94--97\% of chest rows. Every own write lowers its own answer's logit margin more than any competitor raises it ($O^m_q$ from $\cfPairsGemmaFourOmNear$ to $\cfPairsGemmaFourOmFar$, all intervals below zero). MedGemma 4B and 27B differ the same way (Pneumothorax $\cfOMedFourPneu$ versus $\cfOMedTwentySevenPneu$ and Edema $\cfOMedFourEdema$ versus $\cfOMedTwentySevenEdema$ on CheXpert). LLaVA-1.5 7B and 13B own no concepts on either chest set, yet own \cfLlavaSevenCoco{} and \cfLlavaThirteenCoco{} COCO objects with the same write. The chest cells these readers own (Gemma~3 12B: Nodule on NIH, Edema on CheXpert; MedGemma 4B: Cardiomegaly and Edema on CheXpert) carry the least evidential weight. The reader effect spans the whole matrix, regardless of which cell crosses zero.

**after**

Gemma~3 shares one SigLIP tower across its 4B, 12B, and 27B checkpoints. MedGemma shares one MedSigLIP tower across 4B and 27B. LLaVA-1.5 shares one CLIP tower across 7B and 13B. We intervene primarily at the tower's final block. Within each family, pooled features, fitted probes, and write vectors are therefore identical across sizes (bit-identical for Gemma~3 4B and 12B and for LLaVA-1.5; equal within one bf16 rounding step for Gemma~3 27B and MedGemma 27B, direction cosines above $0.9987$). Readability is identical across sizes by construction. The connector and language model determine every difference in Figure~\ref{fig:readers} through how they read the same written representation. These differences are large relative to every noise source in the grid (Table~\ref{tab:cf-pairs}). Under the identical write on the same 600 patients, Gemma~3 4B and 12B differ in $O_q$ by $\cfPairDeltaEffFourTwelve$ $[\cfPairDeltaEffFourTwelveLo,\cfPairDeltaEffFourTwelveHi]$ on NIH Effusion. The difference between 12B and 27B is $\cfPairDeltaEffTwelveTwentySeven$ $[\cfPairDeltaEffTwelveTwentySevenLo,\cfPairDeltaEffTwelveTwentySevenHi]$. In \cfPairsFiveOrSix{} of the \cfPairsN{} shared-tower pair--dataset combinations, the paired patient-bootstrap interval for $\Delta O_q$ excludes zero for five or six of the six concepts. The median $|\Delta O_q|$ is \cfPairsRatioMin{}--\cfPairsRatioMax{} times the families' refit standard deviation (up to \cfPairsRatioSingleMax{} times for single concepts). In 12B, competing directions own the chest answers outright ($O_{\mathrm{Effusion}}=\cfOGemmaTwelveEff$ on both chest sets, $O_{\mathrm{Mass}}=\cfOGemmaTwelveMass$, $O_{\mathrm{Pneumothorax}}=\cfOGemmaTwelvePneu$). In 27B, no direction moves these answers ($\cfOGemmaTwentySevenEff$, $\cfOGemmaTwentySevenMass$, $\cfOGemmaTwentySevenPneu$). In 4B, the clean answer is yes on 94--97\% of chest rows. Every own write lowers its own answer's logit margin more than any competing write raises it ($O^m_q$ from $\cfPairsGemmaFourOmNear$ to $\cfPairsGemmaFourOmFar$, all intervals below zero). MedGemma 4B and 27B differ in the same way (Pneumothorax $\cfOMedFourPneu$ versus $\cfOMedTwentySevenPneu$ and Edema $\cfOMedFourEdema$ versus $\cfOMedTwentySevenEdema$ on CheXpert). LLaVA-1.5 7B and 13B own no concepts on either chest set, yet own \cfLlavaSevenCoco{} and \cfLlavaThirteenCoco{} COCO objects with the same write. The chest cells these readers own (Gemma~3 12B: Nodule on NIH, Edema on CheXpert; MedGemma 4B: Cardiomegaly and Edema on CheXpert) carry the least evidential weight. The reader effect spans the whole matrix, regardless of which cell crosses zero.

## 5_results paragraph 6 — ACCEPTED

**before**

On the chest sets, ownership is concentrated in a few readers and varies by concept and dataset (Figure~\ref{fig:ladders}; Table~\ref{tab:cf-families}). Qwen2.5-VL owns nothing at 3B and 7B. It owns Cardiomegaly at 32B and Atelectasis ($O=0.54$), Cardiomegaly, and Mass at 72B; Effusion is anti-owned at every size. Lingshu, the medical Qwen2.5-VL derivative, owns three CheXpert concepts at 7B and five at 32B, the grid's largest clinical ownership. InternVL3.5 answers every NIH question at every size. Its writes stay within $0.04$ of zero on the chest sets. LLaVA-1.5 reads the chest concepts, answers none above chance, and owns none.

**after**

On the chest sets, ownership is concentrated in a few readers and varies by concept and dataset (Figure~\ref{fig:ladders}; Table~\ref{tab:cf-families}). Qwen2.5-VL owns no concepts at 3B or 7B. At 32B, it owns Cardiomegaly. At 72B, it owns Atelectasis ($O=0.54$), Cardiomegaly, and Mass. Effusion is anti-owned at every size. Lingshu, the medical Qwen2.5-VL derivative, owns three CheXpert concepts at 7B and five at 32B, the grid's highest clinical ownership. InternVL3.5 answers every NIH question at every size. Its writes remain within $0.04$ of zero on the chest sets. LLaVA-1.5 reads the chest concepts but answers none above chance and owns none.

## 5_results paragraph 7 — ACCEPTED

**before**

Across the grid, the reference family and additional controls behave as designed (Figure~\ref{fig:overview}d; Table~\ref{tab:cf-controls}). Owned concept writes rank 1--6 among the 120 compared effects. On the chest sets, the concept write typically ranks in the top decile.

**after**

The reference family and additional controls behave as designed across the grid (Figure~\ref{fig:overview}d; Table~\ref{tab:cf-controls}). Owned concept writes rank 1--6 among the 120 compared effects. The concept write typically ranks in the top decile on the chest sets.

## 5_results paragraph 8 — ACCEPTED

**before**

\paragraph{Geometry, scale, saturation, refits, numerics.} No measurement alternative accounts for the deficit (Tables~\ref{tab:cf-geometry}--\ref{tab:cf-validation}; Appendix~\ref{app:cf-robustness}). The strongest competitor matches the most similar direction or the most co-occurring label at chance rate. Verdicts agree on the logit scale in \cfScaleAgree{} of \cfScaleAgreeN{} cells. COCO blocks are more saturated than chest blocks. Restricting every cell to unsaturated rows preserves all \cfChestOwned{} owned chest cells. Refits move $O_q$ by a median of \cfScaleRefitMedian{}. Column selectivity, the share of a direction's absolute effect that lands on its own question, is \cfValColSelNih{} on NIH and \cfValColSelChex{} on CheXpert against \cfValColSelCoco{} on COCO. On CheXpert, rows with a known label retain the sign of $O_q$ in \cfValKnownSignAgree{} of \cfValKnownN{} cells. Rescoring the write grid with fp32 weights and batch size one changes $W$ by at most \cfPrecisionMaxDW{} and leaves every verdict unchanged in the \cfPrecisionBlocks{} block rescored so far (Table~\ref{tab:cf-precision}).

**after**

\paragraph{Geometry, scale, saturation, refits, numerics.} No measurement alternative explains the deficit (Tables~\ref{tab:cf-geometry}--\ref{tab:cf-validation}; Appendix~\ref{app:cf-robustness}). The strongest competitor matches the most similar direction or the most co-occurring label at chance rate. Verdicts agree on the logit scale in \cfScaleAgree{} of \cfScaleAgreeN{} cells. COCO blocks are more saturated than chest blocks. Restricting every cell to unsaturated rows preserves all \cfChestOwned{} owned chest cells. Refits change $O_q$ by a median of \cfScaleRefitMedian{}. Column selectivity measures the share of a direction's absolute effect that lands on its own question. It is \cfValColSelNih{} on NIH and \cfValColSelChex{} on CheXpert, versus \cfValColSelCoco{} on COCO. On CheXpert, rows with a known label retain the sign of $O_q$ in \cfValKnownSignAgree{} of \cfValKnownN{} cells. Rescoring the write grid with fp32 weights and batch size one changes $W$ by at most \cfPrecisionMaxDW{}. Every verdict remains unchanged in the \cfPrecisionBlocks{} rescored block (Table~\ref{tab:cf-precision}).

## 5_results paragraph 9 — ACCEPTED

**before**

\paragraph{Alternative direction constructions.} The deficit is not a property of the logistic normal. Four other directions built from the same projected, train-scaled features follow the same write protocol (Table~\ref{tab:cf-altdir}): the difference of class means, the Haufe pattern $\Sigma\boldsymbol\beta_c$ \citep{haufe2014interpretation}, the normal orthogonalised against the other five normals, and the normal refitted on features residualised against the other five labels. Across the \cfAltdirBlocks{} blocks scored so far (\cfAltdirBlocksPerDs{} per dataset), every construction owns \cfAltdirChestPctMin{}--\cfAltdirChestPctMax\% of chest cells and \cfAltdirCocoPctMin{}--\cfAltdirCocoPctMax\% of COCO cells. The difference-of-means and pattern directions have cosine $0.1$--$0.4$ to the logistic normal. They own more NIH cells than the logistic normal and fewer COCO cells, and the chest-to-COCO gap holds under each. At least one construction owns \cfAltdirNihAny{} of \cfAltdirNihN{} cells on NIH and \cfAltdirChexAny{} of \cfAltdirChexN{} on CheXpert, against \cfAltdirCocoAny{} of \cfAltdirCocoN{} on COCO. Enlarging the clinical family with every further dataset label that has enough support keeps 5 of the 6 owned cells in the 8 blocks scored (Table~\ref{tab:cf-extcomp}). Writing the logistic directions with per-token weights, by the softmax of each token's probe score or only on the top quarter of tokens at four times the weight (twice the Frobenius norm of the uniform write), owns 0 of 6 chest concepts and 6 of 6 COCO objects in Qwen2.5-VL-7B and Qwen3-VL-8B (Table~\ref{tab:cf-tokenw}). Concentrating the write where the probe reads does not create a clinical handle.

**after**

\paragraph{Alternative direction constructions.} The deficit is not specific to the logistic normal. Four alternative directions use the same projected, train-scaled features and follow the same write protocol (Table~\ref{tab:cf-altdir}): the difference of class means, the Haufe pattern $\Sigma\boldsymbol\beta_c$ \citep{haufe2014interpretation}, the normal orthogonalised against the other five normals, and the normal refitted on features residualised against the other five labels. Across the \cfAltdirBlocks{} blocks scored so far (\cfAltdirBlocksPerDs{} per dataset), every construction owns \cfAltdirChestPctMin{}--\cfAltdirChestPctMax\% of chest cells and \cfAltdirCocoPctMin{}--\cfAltdirCocoPctMax\% of COCO cells. The difference-of-means and pattern directions have cosine $0.1$--$0.4$ to the logistic normal. They own more NIH cells and fewer COCO cells than the logistic normal. The chest-to-COCO gap holds under each. At least one construction owns \cfAltdirNihAny{} of \cfAltdirNihN{} cells on NIH and \cfAltdirChexAny{} of \cfAltdirChexN{} on CheXpert, against \cfAltdirCocoAny{} of \cfAltdirCocoN{} on COCO. Extending the clinical family to every additional dataset label with enough support retains 5 of the 6 owned cells in the 8 scored blocks (Table~\ref{tab:cf-extcomp}). We weight writes of the logistic directions by the softmax of each token's probe score or write only to the top quarter of tokens at four times the weight (twice the Frobenius norm of the uniform write). These writes own 0 of 6 chest concepts and 6 of 6 COCO objects in Qwen2.5-VL-7B and Qwen3-VL-8B (Table~\ref{tab:cf-tokenw}). Concentrating the write where the probe reads does not create a clinical handle.

## 5_results paragraph 10 — ACCEPTED

**before**

\paragraph{The direction the reader responds to.} The reader's handle lies in the same representation. For each question, we fit a ridge regression of the model's own clean answer margin on the same features (3{,}000 training rows). We lift it like a probe normal and write it at the same dose against the same reference family (Table~\ref{tab:cf-ansdir}). This answer direction $a_q$ is a handle where the label direction is not. Across the \cfAnsChestN{} chest cells of the \cfAnsBlocks{} blocks scored (Table~\ref{tab:cf-rescue}), $a_q$ is owned in \cfAnsChestOwnedA{} where the label direction is owned in \cfAnsChestOwnedLabel{}, and its write beats the strongest logistic competitor in \cfAnsChestBeats{}. In Qwen2.5-VL-7B, it is owned for \cfAnsQwenOwnedNih{} of six NIH concepts (Effusion $O^a=\cfAnsQwenEffO$ with an own write of $\cfAnsQwenEffW$) and all \cfAnsQwenOwnedChex{} CheXpert concepts; in Lingshu 32B, for \cfAnsLingOwnedNih{} NIH and \cfAnsLingOwnedChex{} CheXpert concepts; in Gemma~3 12B, for \cfAnsGemOwnedNih{} NIH and \cfAnsGemOwnedChex{} CheXpert concepts. It differs from the probe normal: raw cosine is $\cfAnsCosQwenMin$--$\cfAnsCosQwenMax$ in Qwen2.5-VL-7B and $\cfAnsCosLingMin$--$\cfAnsCosLingMax$ in Lingshu 32B on the chest sets, against $\cfAnsCosCocoMin$--$\cfAnsCosCocoMax$ on COCO. Whitening by the training covariance gives $\cfValCosSigmaChestMin$--$\cfValCosSigmaChestMax$ against $\cfValCosSigmaCoco$. The answer direction reads the label worse than the probe and the model's own answer better (Table~\ref{tab:cf-validation}). On natural images the readable and the writable direction coincide; on chest radiographs the model answers along a direction the probe does not read.

**after**

\paragraph{The direction the reader responds to.} The reader's handle lies in the same representation. For each question, we fit the model's own clean answer margin by ridge regression on the same features (3{,}000 training rows). We lift the fitted direction like a probe normal and write it at the same dose against the same reference family (Table~\ref{tab:cf-ansdir}). This answer direction $a_q$ is a handle where the label direction is not. Across the \cfAnsChestN{} chest cells of the \cfAnsBlocks{} scored blocks (Table~\ref{tab:cf-rescue}), $a_q$ is owned in \cfAnsChestOwnedA{} and the label direction in \cfAnsChestOwnedLabel{}. Its write beats the strongest logistic competitor in \cfAnsChestBeats{}. In Qwen2.5-VL-7B, it is owned for \cfAnsQwenOwnedNih{} of six NIH concepts (Effusion $O^a=\cfAnsQwenEffO$ with an own write of $\cfAnsQwenEffW$) and all \cfAnsQwenOwnedChex{} CheXpert concepts. In Lingshu 32B, it is owned for \cfAnsLingOwnedNih{} NIH and \cfAnsLingOwnedChex{} CheXpert concepts. In Gemma~3 12B, it is owned for \cfAnsGemOwnedNih{} NIH and \cfAnsGemOwnedChex{} CheXpert concepts. Its raw cosine with the probe normal is $\cfAnsCosQwenMin$--$\cfAnsCosQwenMax$ in Qwen2.5-VL-7B and $\cfAnsCosLingMin$--$\cfAnsCosLingMax$ in Lingshu 32B on the chest sets, against $\cfAnsCosCocoMin$--$\cfAnsCosCocoMax$ on COCO. Whitening by the training covariance gives $\cfValCosSigmaChestMin$--$\cfValCosSigmaChestMax$ against $\cfValCosSigmaCoco$. The answer direction reads the label worse than the probe but reads the model's own answer better (Table~\ref{tab:cf-validation}). On natural images, the readable and writable directions coincide. On chest radiographs, the model answers along a direction the probe does not read.

## app_campaign paragraph 11 — ACCEPTED

**before**

\paragraph{Labels.} Concept labels are the report-derived labels shipped with each dataset (NIH ChestX-ray14 finding labels; the CheXpert labeler output distributed with CheXpert Plus; COCO instance annotations). A row is positive when the label is 1, negative when it is 0, and \emph{unknown} when the labeler returns uncertain or blank; unknown rows are excluded from probe fitting, selectivity, and answer AUROC, and are written and scored like every other row. Cohorts are fixed manifests drawn with recorded seeds, patient-disjoint between training and test and disjoint from every seed-study cohort; per-concept counts are in Table~\ref{tab:cf-prevalence}.

**after**

\paragraph{Labels.} Concept labels are the report-derived labels shipped with each dataset (NIH ChestX-ray14 finding labels; the CheXpert labeler output distributed with CheXpert Plus; COCO instance annotations). A row is positive for label 1, negative for label 0, and \emph{unknown} when the labeler returns uncertain or blank. We exclude unknown rows from probe fitting, selectivity, and answer AUROC. We write and score them like every other row. Cohorts are fixed manifests drawn with recorded seeds. Training and test cohorts are patient-disjoint. All cohorts are disjoint from every seed-study cohort. Table~\ref{tab:cf-prevalence} gives per-concept counts.

## app_campaign paragraph 12 — ACCEPTED

**before**

\paragraph{Registration.} The protocol was frozen before any block of the grid was scored: the loci, the projection and probe settings, the dose, the reference family, the six templates, the module set, the seven gates, and the three decision rules are those of the registered protocol file and its code commit, and the seed study's crossover and confirmation analyses were registered before they were run (Appendix~\ref{app:seed}). Everything reported per block is produced by one analysis pass from the packaged outcomes; the leaderboard, the write-flow share, and the per-image examples are descriptive summaries of those outputs added after the grid was complete.

**after**

\paragraph{Registration.} The protocol was frozen before any grid block was scored. The loci, projection and probe settings, dose, reference family, six templates, module set, seven gates, and three decision rules are those of the registered protocol file and its code commit. The seed study's crossover and confirmation analyses were registered before they were run (Appendix~\ref{app:seed}). One analysis pass over the packaged outcomes produces everything reported per block. The leaderboard, write-flow share, and per-image examples are descriptive summaries of these outputs, added after the grid was complete.

## app_campaign paragraph 13 — ACCEPTED

**before**

\paragraph{Artifact.} The release contains the evaluation code and adapters, the registered protocol file, the cohort manifests with their seeds, the fitted directions, the per-image outcomes of every module (write, dose, refit, locus, and template rows), every block's scorecard and coverage file, and the rendered prompts; a new checkpoint is added with one adapter that names its consumed block and passes the gates.

**after**

\paragraph{Artifact.} The release contains evaluation code and adapters, the registered protocol file, cohort manifests with their seeds, fitted directions, per-image outcomes for every module (write, dose, refit, locus, and template rows), every block's scorecard and coverage file, and rendered prompts. Adding a new checkpoint requires one adapter that names the block it consumes and passes the gates.

## app_campaign paragraph 14 — ACCEPTED

**before**

For Llama~3.2 Vision the projector also reads five intermediate local-layer outputs that bypass the final global block; for LLaVA the final CLIP block is computed but not consumed, so the consumed block is the penultimate one. Gate (C) verifies for every checkpoint that the write reaches the consumer and the answer logits and changes nothing outside the consumed tokens.

**after**

For Llama~3.2 Vision, the projector also reads five intermediate local-layer outputs that bypass the final global block. LLaVA computes but does not consume the final CLIP block; it consumes the penultimate block. Gate (C) verifies for every checkpoint that the write reaches the consumer and the answer logits and changes nothing outside the consumed tokens.

## app_campaign paragraph 15 — ACCEPTED

**before**

All \cfWriteBlocks{} completed blocks pass checks (A), (B), (C), and (G) with zero determinism error, zero change at $\alpha=0$, and fp32-versus-model logit differences below 0.13. Check (D) exceeds the 0.25-logit tolerance for the Qwen3-VL, Gemma~3, MedGemma, InternVL, and Llama families (up to 1.84 logits for Gemma~3 12B); these are recorded as declared deviations and neutralised by the fixed batch composition. Check (E) fails the A/B templates for LLaVA-1.5-7B and LLaVA-Med, the B-mapped templates for Qwen2.5-VL-3B and LLaVA-1.5-13B, and the yes/no and A-mapped templates for Llama~3.2 Vision 11B on all datasets; the affected cells are recorded as ineligible. Two checkpoints answer ``yes'' to nearly every chest question (Gemma~3 4B: clean $P(\mathrm{yes})>0.99$ on 94--97\% of rows), which the answerability grade records; three (LLaVA-1.5 7B/13B, LLaVA-Med) answer at chance.

**after**

All \cfWriteBlocks{} completed blocks pass checks (A), (B), (C), and (G) with zero determinism error, zero change at $\alpha=0$, and fp32-versus-model logit differences below 0.13. In check (D), the Qwen3-VL, Gemma~3, MedGemma, InternVL, and Llama families exceed the 0.25-logit tolerance (up to 1.84 logits for Gemma~3 12B). We record these as declared deviations and neutralise them through fixed batch composition. Check (E) fails for the A/B templates for LLaVA-1.5-7B and LLaVA-Med, the B-mapped templates for Qwen2.5-VL-3B and LLaVA-1.5-13B, and the yes/no and A-mapped templates for Llama~3.2 Vision 11B on all datasets. We record the affected cells as ineligible. Two checkpoints answer ``yes'' to nearly every chest question (Gemma~3 4B: clean $P(\mathrm{yes})>0.99$ on 94--97\% of rows). The answerability grade records this behaviour. Three checkpoints (LLaVA-1.5 7B/13B, LLaVA-Med) answer at chance.

## app_campaign paragraph 16 — ACCEPTED

**before**

Nineteen checkpoints are complete on all three datasets with every module (Table~\ref{tab:cf-coverage}; the write matrix, calibration, the five additional templates, the dose sweep, the two refits, and the connector locus on NIH and COCO; the write matrix and calibration on CheXpert); Llama~3.2 Vision 11B is complete with its yes/no modules ineligible; Qwen2.5-VL-72B is complete on NIH; the 72B COCO/CheXpert blocks and Llama~3.2 Vision 90B were not run (compute budget), and InternVL3.5-38B is complete on all three datasets. The campaign consumed about 3,000 A100-hours; a chest block of a 7B checkpoint takes about ten GPU-hours.

**after**

Nineteen checkpoints are complete on all three datasets with every module (Table~\ref{tab:cf-coverage}): the write matrix, calibration, the five additional templates, the dose sweep, the two refits, and the connector locus on NIH and COCO; the write matrix and calibration on CheXpert. Llama~3.2 Vision 11B is complete with its yes/no modules ineligible. Qwen2.5-VL-72B is complete on NIH. The 72B COCO/CheXpert blocks and Llama~3.2 Vision 90B were not run (compute budget). InternVL3.5-38B is complete on all three datasets. The campaign consumed about 3,000 A100-hours; a chest block of a 7B checkpoint takes about ten GPU-hours.

## app_campaign paragraph 17 — ACCEPTED

**before**

Tables~\ref{tab:cf-own-nih}--\ref{tab:cf-own-coco} give the ownership contrast of every concept cell; the structure of the write matrices behind them is summarised in Figure~\ref{fig:cf-w-structure}, and Figure~\ref{fig:cf-examples} shows the grades on single images.

**after**

Tables~\ref{tab:cf-own-nih}--\ref{tab:cf-own-coco} report the ownership contrast for every concept cell. Figure~\ref{fig:cf-w-structure} summarises the structure of the underlying write matrices. Figure~\ref{fig:cf-examples} shows the grades on single images.

## app_campaign paragraph 18 — ACCEPTED

**before**

Every alternative direction is a vector $\boldsymbol u$ in the projected, train-scaled space and is lifted to the consumed space exactly as the logistic normal, $\hat\wvec=R(\boldsymbol u\oslash\max(\boldsymbol s,10^{-8}))$ normalised (Equation~\ref{eq:lift}). Because $x=R^{\top}\hvec$ with $R$ a fixed Gaussian matrix of near-orthonormal columns, a displacement $R\boldsymbol\delta$ of the consumed representation produces the displacement $R^{\top}R\boldsymbol\delta\approx\boldsymbol\delta$ of the projected features, so the same lift carries classifier normals, class-mean displacements, and patterns alike; the difference-of-means and pattern vectors are computed in the projected space and lifted once. The token-weighted variants scale the per-token dose $\alpha\lVert\hvec_{it}\rVert$ by a weight $w_t$ with mean one over the consumed tokens: the softmax variant keeps the L1 dose of the uniform write and the top-quarter variant ($w_t=4$ on the 25\% highest-scoring tokens) has twice its Frobenius norm.

**after**

Every alternative direction is a vector $\boldsymbol u$ in the projected, train-scaled space. We lift it to the consumed space exactly as the logistic normal, by normalising $\hat\wvec=R(\boldsymbol u\oslash\max(\boldsymbol s,10^{-8}))$ (Equation~\ref{eq:lift}). Here $x=R^{\top}\hvec$, where $R$ is a fixed Gaussian matrix with near-orthonormal columns. A displacement $R\boldsymbol\delta$ of the consumed representation therefore produces the displacement $R^{\top}R\boldsymbol\delta\approx\boldsymbol\delta$ of the projected features. The same lift carries classifier normals, class-mean displacements, and patterns alike. We compute the difference-of-means and pattern vectors in the projected space and lift them once. The token-weighted variants scale the per-token dose $\alpha\lVert\hvec_{it}\rVert$ by a weight $w_t$ with mean one over the consumed tokens. The softmax variant preserves the L1 dose of the uniform write. The top-quarter variant ($w_t=4$ on the 25\% highest-scoring tokens) has twice the uniform write's Frobenius norm.

## app_campaign paragraph 19 — ACCEPTED

**before**

\paragraph{Direction and label geometry.} Neither correlation among the six directions nor co-occurrence of the six report labels explains why a competing direction wins (Table~\ref{tab:cf-geometry}). On NIH, the six directions are as orthogonal as random vectors (mean off-diagonal cosine $\cfGeoCosNih$ against $\cfGeoCosRandom$ for random pairs), and the labels are nearly uncorrelated (mean $\phi=\cfGeoPhiNih$). Yet NIH shows the same off-diagonal dominance as CheXpert, where directions and labels are correlated (mean cosine $\cfGeoCosChex$; Effusion--Consolidation $\cfGeoCosEffCons$, $\phi=\cfGeoPhiEffCons$). The own direction wins in \cfGeoOwnWinsNihPct\% of NIH and \cfGeoOwnWinsChexPct\% of CheXpert cells. The strongest competitor matches the most similar direction at chance rate (\cfGeoChanceCosMin{}--\cfGeoChanceCosMax\%) and the most co-occurring label at chance rate (\cfGeoChanceLabelMin{}--\cfGeoChanceLabelMax\%). Across the \cfGeoOffdiagCells{} chest off-diagonal cells, the competitor's advantage $W_{q,d}-W_{q,q}$ is uncorrelated with cosine (Spearman $\cfGeoRhoAdvCos$, $p=\cfGeoRhoAdvCosP$) and with $\phi$ ($\cfGeoRhoAdvPhi$). Removing each cell's strongest competitor does not restore ownership: \cfGeoTwoAbove{} of \cfGeoTwoAboveN{} chest cells (\cfGeoTwoAbovePct\%) have at least two competitors above the own write. The natural-image families with the highest co-occurrence (person with bicycle, $\phi=\cfGeoFamPhiPersonBicycle$; person with dog; chair with bottle) retain ownership in \cfGeoCocoFamMinPct{}--\cfGeoCocoFamMaxPct\% of cells when the competitor set is restricted to them. In the seed cell, Nodule is the \emph{least} similar of the five directions to Effusion (cosine $\cfSeedCosNodule$). It still moves the Effusion answer more than Effusion's own direction ($\cfSeedWNodule$ against $\cfSeedWEff$). The most similar direction, Atelectasis (cosine $\cfSeedCosAtel$), moves it by $\cfSeedWAtel$.

**after**

\paragraph{Direction and label geometry.} Neither correlations among the six directions nor co-occurrence of the six report labels explains why a competing direction wins (Table~\ref{tab:cf-geometry}). On NIH, the six directions are as orthogonal as random vectors (mean off-diagonal cosine $\cfGeoCosNih$ against $\cfGeoCosRandom$ for random pairs). The labels are nearly uncorrelated (mean $\phi=\cfGeoPhiNih$). Yet NIH shows the same off-diagonal dominance as CheXpert, where directions and labels are correlated (mean cosine $\cfGeoCosChex$; Effusion--Consolidation $\cfGeoCosEffCons$, $\phi=\cfGeoPhiEffCons$). The own direction wins in \cfGeoOwnWinsNihPct\% of NIH and \cfGeoOwnWinsChexPct\% of CheXpert cells. The strongest competitor matches the most similar direction at chance rate (\cfGeoChanceCosMin{}--\cfGeoChanceCosMax\%) and the most co-occurring label at chance rate (\cfGeoChanceLabelMin{}--\cfGeoChanceLabelMax\%). Across the \cfGeoOffdiagCells{} off-diagonal chest cells, the competitor's advantage $W_{q,d}-W_{q,q}$ is uncorrelated with cosine (Spearman $\cfGeoRhoAdvCos$, $p=\cfGeoRhoAdvCosP$) and with $\phi$ ($\cfGeoRhoAdvPhi$). Removing each cell's strongest competitor does not restore ownership: \cfGeoTwoAbove{} of \cfGeoTwoAboveN{} chest cells (\cfGeoTwoAbovePct\%) have at least two competitors above the own write. The natural-image families with the highest co-occurrence (person with bicycle, $\phi=\cfGeoFamPhiPersonBicycle$; person with dog; chair with bottle) retain ownership in \cfGeoCocoFamMinPct{}--\cfGeoCocoFamMaxPct\% of cells when competitors are restricted to those families. Of the five directions in the seed cell, Nodule is the \emph{least} similar to Effusion (cosine $\cfSeedCosNodule$). It still moves the Effusion answer more than Effusion's own direction ($\cfSeedWNodule$ against $\cfSeedWEff$). The most similar direction, Atelectasis (cosine $\cfSeedCosAtel$), moves it by $\cfSeedWAtel$.

## app_campaign paragraph 20 — ACCEPTED

**before**

\paragraph{Scale, saturation, refits, and numerics.} The grades hold on the logit scale: ownership recomputed from the margin $\ell_{\mathrm{yes}}-\ell_{\mathrm{no}}$ agrees with the probability-scale verdict in \cfScaleAgree{} of \cfScaleAgreeN{} cells; every owned cell keeps $O^m_q>0$ and \cfScaleOwnedKeepFull{} of \cfScaleOwned{} keep the full verdict under the same rules (Table~\ref{tab:cf-scale}). Saturation runs counter to a ceiling explanation: COCO blocks have far more rows at $P(\mathrm{yes})>0.99$ or $<0.01$ than chest blocks (median block share $\cfScaleSatCoco$ against $\cfScaleSatNih$ and $\cfScaleSatChex$). Restricting every cell to its unsaturated rows preserves $O_q>0$ in all \cfScaleUnsatOwned{} owned chest cells and in \cfScaleUnsatOtherPos{} of the \cfScaleUnsatOther{} others. The same pattern holds in the clean-No, clean-Yes, label-present, and label-absent strata. Refitting the probes on resampled training patients changes $O_q$ by a median of $\cfScaleRefitMedian$. The bf16 batch effects declared at the gates (candidate-logit differences of $\cfScaleBatchMin$--$\cfScaleBatchMax$) leave the verdicts unchanged: the margin sign agrees in \cfScaleBatchAgree{} of \cfScaleBatchN{} blocks, and the \cfScaleBatchDisagree{} disagreeing blocks contain no owned cell. Re-scoring the clean baseline inside a second module gives bitwise agreement in 100\% of rows for \cfScaleBaselineIdentical{} of \cfScaleBaselineN{} blocks and in at least \cfScaleBaselineRestPct\% for the rest, with $|\Delta P(\mathrm{yes})|>0.1$ in at most \cfScaleBaselineDpPct\% of rows. Writes at the connector locus are an order of magnitude smaller than at the primary locus (median $|W_{q,q}|$ $\cfConnWqqNih$ against $\cfPrimWqqNih$ on NIH and $\cfConnWqqCoco$ against $\cfPrimWqqCoco$ on COCO).

**after**

\paragraph{Scale, saturation, refits, and numerics.} The grades hold on the logit scale. Ownership recomputed from the margin $\ell_{\mathrm{yes}}-\ell_{\mathrm{no}}$ agrees with the probability-scale verdict in \cfScaleAgree{} of \cfScaleAgreeN{} cells. Every owned cell retains $O^m_q>0$, and \cfScaleOwnedKeepFull{} of \cfScaleOwned{} retain the full verdict under the same rules (Table~\ref{tab:cf-scale}). Saturation counters a ceiling explanation: COCO blocks have far more rows at $P(\mathrm{yes})>0.99$ or $<0.01$ than chest blocks (median block share $\cfScaleSatCoco$ against $\cfScaleSatNih$ and $\cfScaleSatChex$). Restricting each cell to its unsaturated rows preserves $O_q>0$ in all \cfScaleUnsatOwned{} owned chest cells and in \cfScaleUnsatOtherPos{} of the \cfScaleUnsatOther{} others. The same pattern holds in the clean-No, clean-Yes, label-present, and label-absent strata. Refitting the probes on resampled training patients changes $O_q$ by a median of $\cfScaleRefitMedian$. The bf16 batch effects declared at the gates (candidate-logit differences of $\cfScaleBatchMin$--$\cfScaleBatchMax$) leave the verdicts unchanged. The margin sign agrees in \cfScaleBatchAgree{} of \cfScaleBatchN{} blocks; the \cfScaleBatchDisagree{} disagreeing blocks contain no owned cell. Re-scoring the clean baseline in a second module gives bitwise agreement in 100\% of rows for \cfScaleBaselineIdentical{} of \cfScaleBaselineN{} blocks and in at least \cfScaleBaselineRestPct\% for the rest, with $|\Delta P(\mathrm{yes})|>0.1$ in at most \cfScaleBaselineDpPct\% of rows. Writes at the connector locus are an order of magnitude smaller than those at the primary locus (median $|W_{q,q}|$ $\cfConnWqqNih$ against $\cfPrimWqqNih$ on NIH and $\cfConnWqqCoco$ against $\cfPrimWqqCoco$ on COCO).

## app_campaign paragraph 21 — ACCEPTED

**before**

Table~\ref{tab:cf-geometry} gives the direction- and label-geometry statistics, Table~\ref{tab:cf-scale} the scale, saturation, refit, connector, and batch statistics behind Section~\ref{sec:robust-geometry}, Table~\ref{tab:cf-pairs} the paired shared-tower differences behind Section~\ref{sec:reader}, and Tables~\ref{tab:cf-altdir}--\ref{tab:cf-tokenw} the alternative directions, the answer-direction oracle, the extended competitor family, and the token-weighted writes.

**after**

Table~\ref{tab:cf-geometry} reports direction- and label-geometry statistics. Table~\ref{tab:cf-scale} reports the scale, saturation, refit, connector, and batch statistics supporting Section~\ref{sec:robust-geometry}. Table~\ref{tab:cf-pairs} reports the paired shared-tower differences supporting Section~\ref{sec:reader}. Tables~\ref{tab:cf-altdir}--\ref{tab:cf-tokenw} report the alternative directions, answer-direction oracle, extended competitor family, and token-weighted writes.

## app_campaign paragraph 22 — ACCEPTED

**before**

Figure~\ref{fig:cf-w-structure} condenses the $6\times6$ write matrices of every completed checkpoint. The top row is the cell-wise median matrix per dataset. The bottom row shows where the write goes: each written direction on the left is joined to the questions it moves on the right, with band width proportional to the mean positive answer change across checkpoints; bands that land on the writer's own question are coloured and bands that land elsewhere are grey. The share of the write that lands on its own question is \cfOwnShareNih\% on NIH and \cfOwnShareChex\% on CheXpert against \cfOwnShareCoco\% on COCO; on the chest sets the largest sinks are the Cardiomegaly question on NIH and the Edema and Effusion questions on CheXpert, which receive writes from every direction.

**after**

Figure~\ref{fig:cf-w-structure} summarises the $6\times6$ write matrices of every completed checkpoint. The top row shows the cell-wise median matrix for each dataset. The bottom row traces each written direction on the left to the questions it moves on the right. Band width is proportional to the mean positive answer change across checkpoints. Bands reaching the writer's own question are coloured; bands reaching other questions are grey. The share of the write reaching its own question is \cfOwnShareNih\% on NIH and \cfOwnShareChex\% on CheXpert against \cfOwnShareCoco\% on COCO. On the chest sets, the largest sinks are the Cardiomegaly question on NIH and the Edema and Effusion questions on CheXpert. These questions receive writes from every direction.

## app_campaign paragraph 23 — ACCEPTED

**before**

Figure~\ref{fig:cf-examples} shows the grades on single test images. For a chest example the pool is the test rows on which the finding is labelled present, the probe reads it (probability above $0.5$), and the clean answer is No; for a natural-image example the pool is the rows on which the probe reads the label correctly and the clean answer is No. Within the pool, rows are split by whether the concept's own write or the strongest competing write raises the answer more, and the row shown is the one nearest the median gap among rows where either write raises the answer by at least $0.2$; the last line of each example gives how many rows of the pool share its ordering, so that each example carries its own base rate.

**after**

Figure~\ref{fig:cf-examples} shows grades on individual test images. For a chest example, the pool comprises test rows where the finding is labelled present, the probe reads it (probability above $0.5$), and the clean answer is No. For a natural-image example, the pool comprises rows where the probe reads the label correctly and the clean answer is No. Rows in each pool are split by whether the concept's own write or the strongest competing write raises the answer more. The row shown is nearest the median gap among rows where either write raises the answer by at least $0.2$. Each example's last line reports how many pool rows share its ordering, giving each example its own base rate.


{"sent": 23, "accepted": 23, "rejected": 0}

# GPT-6-Astra paragraph pass 2026-09-16 21:29

## 0_abstract paragraph 0 — ACCEPTED

**before**

A linear probe reads a clinical concept from a vision-language model's visual representation. Writing the probe direction back into that representation tests whether it is a handle on the concept: it must move the concept's own answer more than every other direction constructed the same way. On chest radiographs, the probe direction almost never passes this test. We define three criteria at the visual block the language model consumes: whether a concept is \emph{readable} (probe AUROC above matched random-label controls), \emph{answerable} (the model's own yes/no answer beats chance), and \emph{owned} (a fixed-dose write along the probe direction moves the concept's answer more than the 95th percentile of 119 random directions, a coordinate-permutation sham, and every competing clinical direction, with simultaneous max-$T$ bounds). Across \cfCheckpoints{} checkpoints from nine model families (3B to 72B) on NIH ChestX-ray14 and CheXpert Plus, clinical concepts are readable in \cfChestReadablePct\% of checkpoint--concept cells, answerable in \cfChestAnswerablePct\%, and owned in only \cfChestOwnedPct\%. Two measurements make up this gap. At the fixed dose, the probe direction clears the random and sham references in \cfChestRefMet{} of the \cfChestAnswerableN{} chest cells. Among those \cfChestRefMet{} cells, \cfChestOwned{} are owned, \cfChestRefStrong{} lose to a competing clinical direction, and \cfChestRefUnres{} are unresolved. A competing clinical direction moves the concept's answer more than its own direction does in \cfChestCompetitor{} cells, and \cfChestBelowStrong{} of these \cfChestCompetitor{} verdicts arise in cells whose own write does not clear the references. The seed result reproduces on new patients (ownership $\cfSeedEffO$, 95\% CI $[\cfSeedEffOLo,\cfSeedEffOHi]$). On COCO objects, the same procedure owns \cfCocoOwnedPct\% of cells, and \cfOwnShareCoco\% of the positive answer change a write produces lands on its own question, against \cfOwnShareNih\% on NIH and \cfOwnShareChex\% on CheXpert. The write mechanism is intact. The deficit is specific to clinical concepts. A direction fitted to the model's own clean answer margin provides a handle in the same representation. It is owned where the probe direction is not. On the chest sets it reads the clinical label less accurately than the probe (median AUROC $\cfValAurocAnsChest$ against $\cfValAurocProbeChest$) and the model's own answer more accurately ($\cfValAurocAnsCleanChest$ against $\cfValAurocProbeCleanChest$). On chest radiographs, this answer direction is nearly orthogonal to the probe direction in raw model space (cosine $\cfAnsCosChestMed$ against $\cfAnsCosCocoMed$ on natural images, median over concepts within each block, then over blocks) and remains far from it under the training-feature covariance (whitened cosine $\cfValCosSigmaChestMin$--$\cfValCosSigmaChestMax$ against $\cfValCosSigmaCoco$, median over cells). Gemma~3 and MedGemma checkpoints of different sizes share one vision tower and therefore have identical probes and write vectors. Yet they own different concepts. The reader of the representation---the connector and language model---decides what a written direction does; the representation does not. The protocol, acceptance gates, and per-checkpoint leaderboard form a reusable benchmark for concept writability in medical vision-language models.

**after**

A linear probe reads a clinical concept from a vision-language model's visual representation. Writing the probe direction back into that representation tests whether it is a handle on the concept: this write must move the concept's answer more than writes along every other direction constructed the same way. On chest radiographs, the probe direction almost never passes this test. We define three criteria at the visual block the language model consumes: whether a concept is \emph{readable} (probe AUROC above matched random-label controls), \emph{answerable} (the model's own yes/no answer beats chance), and \emph{owned} (a fixed-dose write along the probe direction moves the concept's answer more than the 95th percentile of 119 random directions, a coordinate-permutation sham, and every competing clinical direction, with simultaneous max-$T$ bounds). Across \cfCheckpoints{} checkpoints from nine model families (3B to 72B) on NIH ChestX-ray14 and CheXpert Plus, clinical concepts are readable in \cfChestReadablePct\% of checkpoint--concept cells, answerable in \cfChestAnswerablePct\%, and owned in only \cfChestOwnedPct\%. Two measurements characterise this gap. At the fixed dose, the probe direction clears the random and sham references in \cfChestRefMet{} of the \cfChestAnswerableN{} chest cells. Of those \cfChestRefMet{} cells, \cfChestOwned{} are owned, \cfChestRefStrong{} lose to a competing clinical direction, and \cfChestRefUnres{} are unresolved. A competing clinical direction moves the concept's answer more than its own direction does in \cfChestCompetitor{} cells. Of these \cfChestCompetitor{} verdicts, \cfChestBelowStrong{} occur where the concept's own write fails to clear the references. The seed result reproduces on new patients (ownership $\cfSeedEffO$, 95\% CI $[\cfSeedEffOLo,\cfSeedEffOHi]$). On COCO objects, the same procedure yields ownership in \cfCocoOwnedPct\% of cells. There, \cfOwnShareCoco\% of the positive answer change a write produces lands on its own question, against \cfOwnShareNih\% on NIH and \cfOwnShareChex\% on CheXpert. The write mechanism is intact. The deficit is specific to clinical concepts. A direction fitted to the model's own clean answer margin provides a handle in the same representation. It is owned where the probe direction is not. On the chest sets, it reads the clinical label less accurately than the probe (median AUROC $\cfValAurocAnsChest$ against $\cfValAurocProbeChest$). It reads the model's own answer more accurately ($\cfValAurocAnsCleanChest$ against $\cfValAurocProbeCleanChest$). On chest radiographs, this answer direction is nearly orthogonal to the probe direction in raw model space (cosine $\cfAnsCosChestMed$ against $\cfAnsCosCocoMed$ on natural images, median over concepts within each block, then over blocks). It remains far from the probe direction under the training-feature covariance (whitened cosine $\cfValCosSigmaChestMin$--$\cfValCosSigmaChestMax$ against $\cfValCosSigmaCoco$, median over cells). Gemma~3 and MedGemma checkpoints of different sizes share one vision tower and therefore have identical probes and write vectors. Yet they own different concepts. The reader of the representation---the connector and language model---decides what a written direction does; the representation does not. The protocol, acceptance gates, and per-checkpoint leaderboard form a reusable benchmark for concept writability in medical vision-language models.

## 1_introduction paragraph 1 — ACCEPTED

**before**

Linear probes are the standard tool for testing what a vision-language model (VLM) represents: fit a classifier to a chosen layer's activations and read out a concept \citep{alain2017understanding,belinkov2022probing}. In medical imaging, probes often succeed: clinical findings such as pleural effusion or cardiomegaly are linearly decodable from the visual representation of general-purpose and medical VLMs \citep{zhu2026lost,pedersen2026probes}. Activation steering promises the converse: write the probe direction back into the representation to change the model's behaviour \citep{turner2023steering,panickssery2024caa}. The combined promise is a probe direction that serves as a handle on the concept: read it to detect, write it to control. This paper asks whether that handle exists.

**after**

Linear probes are the standard tool for testing what a vision-language model (VLM) represents. They fit a classifier to a chosen layer's activations to read out a concept \citep{alain2017understanding,belinkov2022probing}. In medical imaging, probes often succeed: they linearly decode clinical findings such as pleural effusion or cardiomegaly from the visual representation of general-purpose and medical VLMs \citep{zhu2026lost,pedersen2026probes}. Activation steering promises the converse: write the probe direction back into the representation to change the model's behaviour \citep{turner2023steering,panickssery2024caa}. Together, these methods promise a probe direction that serves as a handle on the concept: read it to detect, write it to control. We ask whether that handle exists.

## 1_introduction paragraph 2 — REJECTED: hedge added

**before**

Showing that steering ``works'' does not answer the question. A write can change an answer for reasons unrelated to the concept's label. Any perturbation of matched size can push a yes/no answer. A direction fitted for one finding can carry a generic abnormality signal shared with other findings \citep{pahde2025navigating}. Random and sham directions test the first possibility. Testing the second requires a comparison steering studies rarely make: write every competing clinical direction at the same locus and dose in the same patients, then ask whether the concept's own direction moves the concept's own answer more than any other direction. We call the outcome \emph{ownership}. A concept with a readable but unowned probe direction is one the model can be read for but not written with---reading is not writing.

**after**

Showing that steering ``works'' does not establish control of the concept. A write can change an answer for reasons unrelated to the concept's label. Any perturbation of matched size can push a yes/no answer. A direction fitted for one finding can carry a generic abnormality signal shared with other findings \citep{pahde2025navigating}. Random and sham directions test the first possibility. Testing the second requires comparing every competing clinical direction written at the same locus and dose in the same patients. Steering studies rarely make this comparison: does the concept's own direction move the concept's own answer more than any other direction? We call the outcome \emph{ownership}. A concept with a readable but unowned probe direction is one the model can be read for but not written with---reading is not writing.

## 1_introduction paragraph 3 — ACCEPTED

**before**

We build an evaluation to test this distinction at scale (Section~\ref{sec:framework}, Figure~\ref{fig:framework}). At the final visual block whose output the language model consumes, we assign each concept three grades. A concept is \emph{readable} if its probe beats matched random-label controls. It is \emph{answerable} if the model's clean yes/no answer beats chance. It is \emph{owned} if a fixed-dose write along its direction beats the 95th percentile of 119 random directions, a coordinate-permutation sham, and all five competing clinical directions under simultaneous max-$T$ bounds. Seven acceptance gates precede block scoring. We evaluate every contrast in one fixed batch composition. Every NIH and COCO block reports dose response, refit stability, a second locus, label-shift controls, and prompt-template contrasts. We evaluate \cfCheckpoints{} checkpoints from nine families (3B--72B; general-purpose and medical) on two chest-radiograph datasets with report-derived labels, NIH ChestX-ray14 and CheXpert Plus, and on COCO objects as a natural-image control (Section~\ref{sec:setup}).

**after**

We build an evaluation that tests this distinction at scale (Section~\ref{sec:framework}, Figure~\ref{fig:framework}). We assign each concept three grades at the final visual block whose output the language model consumes. A concept is \emph{readable} if its probe beats matched random-label controls. It is \emph{answerable} if the model's clean yes/no answer beats chance. It is \emph{owned} if a fixed-dose write along its direction beats the 95th percentile of 119 random directions, a coordinate-permutation sham, and all five competing clinical directions under simultaneous max-$T$ bounds. Seven acceptance gates precede block scoring. All contrasts use one fixed batch composition. Every NIH and COCO block reports dose response, refit stability, a second locus, label-shift controls, and prompt-template contrasts. We evaluate \cfCheckpoints{} checkpoints from nine families (3B--72B; general-purpose and medical) on NIH ChestX-ray14 and CheXpert Plus, two chest-radiograph datasets with report-derived labels, and on COCO objects as a natural-image control (Section~\ref{sec:setup}).

## 1_introduction paragraph 4 — ACCEPTED

**before**

The results show a consistent pattern (Section~\ref{sec:results}, Table~\ref{tab:cf-main}, Figure~\ref{fig:overview}). Clinical concepts are read and answered in most cells but owned in \cfChestOwnedPct\%. Two measured facts produce this result, in order. First, at the fixed dose the probe direction clears the random and sham references in only \cfChestRefMet{} of the \cfChestAnswerableN{} chest cells; in the other \cfChestRefBelow{}, the concept's own write does not clear them. Second, among the \cfChestRefMet{} cells that clear the references, \cfChestOwned{} are owned, \cfChestRefStrong{} lose to a competing clinical direction, and \cfChestRefUnres{} are unresolved. Across all chest cells, a competing clinical direction moves the concept's answer more than its own direction does in \cfChestCompetitor{} cells, and \cfChestBelowStrong{} of these \cfChestCompetitor{} verdicts arise below the references. The seed result reproduces on patients disjoint from every earlier cohort. The same procedure owns \cfCocoOwnedPct\% of COCO cells, so the deficit is specific to clinical concepts, not to the write. A direction fitted to the model's own clean answer margin provides a handle where the probe direction does not. On the chest sets, this answer direction reads the clinical label less accurately than the probe (median AUROC $\cfValAurocAnsChest$ against $\cfValAurocProbeChest$) and the model's own clean answer more accurately ($\cfValAurocAnsCleanChest$ against $\cfValAurocProbeCleanChest$). In raw model space, the cosine between these directions, taken as the median over concepts within a block and then over blocks, is $\cfAnsCosChestMed$ on chest radiographs, ranging $\cfAnsCosChestMin$--$\cfAnsCosChestMax$ across blocks, compared with $\cfAnsCosCocoMed$ on natural images. Whitened by the training-feature covariance, the median cosine over cells is $\cfValCosSigmaChestMin$--$\cfValCosSigmaChestMax$ on the two chest sets and $\cfValCosSigmaCoco$ on COCO. Gemma~3 and MedGemma checkpoints of different sizes share one vision tower. Their probes and write vectors are therefore identical across sizes. Yet the concepts they own change with the language model that reads the write. The reader determines ownership.

**after**

The results show a consistent pattern (Section~\ref{sec:results}, Table~\ref{tab:cf-main}, Figure~\ref{fig:overview}). Clinical concepts are read and answered in most cells but owned in \cfChestOwnedPct\%. Two measured facts explain this result. First, at the fixed dose, the probe direction clears the random and sham references in only \cfChestRefMet{} of the \cfChestAnswerableN{} chest cells; the concept's own write fails to clear them in the other \cfChestRefBelow{}. Second, among the \cfChestRefMet{} cells that clear the references, \cfChestOwned{} are owned, \cfChestRefStrong{} lose to a competing clinical direction, and \cfChestRefUnres{} are unresolved. Across all chest cells, a competing clinical direction moves the concept's answer more than the concept's own direction does in \cfChestCompetitor{} cells. Of these \cfChestCompetitor{} verdicts, \cfChestBelowStrong{} arise below the references. The seed result reproduces on patients disjoint from every earlier cohort. The same procedure owns \cfCocoOwnedPct\% of COCO cells, so the deficit is specific to clinical concepts, not to the write. A direction fitted to the model's own clean answer margin provides a handle where the probe direction does not. On the chest sets, this answer direction reads the clinical label less accurately than the probe (median AUROC $\cfValAurocAnsChest$ against $\cfValAurocProbeChest$). It reads the model's own clean answer more accurately ($\cfValAurocAnsCleanChest$ against $\cfValAurocProbeCleanChest$). In raw model space, the cosine between these directions, taking the median first over concepts within each block and then over blocks, is $\cfAnsCosChestMed$ on chest radiographs, ranging $\cfAnsCosChestMin$--$\cfAnsCosChestMax$ across blocks, compared with $\cfAnsCosCocoMed$ on natural images. After whitening by the training-feature covariance, the median cosine over cells is $\cfValCosSigmaChestMin$--$\cfValCosSigmaChestMax$ on the two chest sets and $\cfValCosSigmaCoco$ on COCO. Gemma~3 and MedGemma checkpoints of different sizes share one vision tower, so their probes and write vectors are identical across sizes. Yet the concepts they own change with the language model that reads the write. The reader determines ownership.

## 1_introduction paragraph 6 — ACCEPTED

**before**

\paragraph{Contributions.} (i) We introduce a concept-writability evaluation for VLMs that grades readability, answerability, and ownership at the consumed visual block with matched clinical competitors, a random--sham reference family, simultaneous bounds, and registered acceptance gates. (ii) We report its first cross-model measurement: \cfCheckpoints{} checkpoints, nine families, three datasets, \cfCells{} scored concept cells, with every control reported per block. (iii) Three findings hold across the grid: clinical concepts are read and answered far more often than they are owned; the write mechanism is intact on natural objects; and the reader of the representation, not the representation, decides ownership. We release the evaluation, scorecards, and write matrices as a benchmark. A new model is added with one adapter naming its consumed block.

**after**

\paragraph{Contributions.} (i) We introduce a concept-writability evaluation for VLMs that grades readability, answerability, and ownership at the consumed visual block. It uses matched clinical competitors, a random--sham reference family, simultaneous bounds, and registered acceptance gates. (ii) We report its first cross-model measurement: \cfCheckpoints{} checkpoints, nine families, three datasets, \cfCells{} scored concept cells. We report every control per block. (iii) Three findings hold across the grid: clinical concepts are read and answered far more often than they are owned; the write mechanism is intact on natural objects; and the reader of the representation, not the representation, decides ownership. We release the evaluation, scorecards, and write matrices as a benchmark. A new model needs one adapter naming its consumed block.

## 3_framework paragraph 1 — ACCEPTED

**before**

We ask three questions about a concept $c$ in a checkpoint $M$ on a dataset with binary labels: whether $c$ is readable from the visual representation consumed by the language model, whether $M$ answers the question about $c$, and whether writing $c$'s own direction moves $c$'s answer more than any alternative. Each question has a fixed estimator and decision rule. We choose nothing after observing the outcome.

**after**

We ask three questions about a concept $c$ in a checkpoint $M$ on a dataset with binary labels: whether $c$ is readable from the visual representation the language model consumes, whether $M$ answers the question about $c$, and whether writing $c$'s own direction moves $c$'s answer more than any alternative. We fix an estimator and decision rule for each question. We choose nothing after observing the outcome.

## 3_framework paragraph 11 — ACCEPTED

**before**

Seven checks run on 16 held-out preflight rows at both loci before block scoring (Table~\ref{tab:cf-gates}): bitwise determinism; identity at $\alpha=0$; reach; batched-versus-single agreement of the answer logits; image-free semantic mapping of every template; throughput; and fp32 recomputation of the answer logits. Reach requires the write to change the consumer's input and answer logits without changing anything outside the consumed tokens. We declare batched-versus-single deviations above $0.25$ and neutralise them by scoring every condition of a question in one fixed batch composition. We mark templates that fail semantic mapping as ineligible and close them as such. We grade a checkpoint whose yes/no template fails on its eligible B-positive template (IB for Llama~3.2 Vision). Its scorecard records the template used. The block's scorecard records every declared deviation.

**after**

Seven checks run on 16 held-out preflight rows at both loci before block scoring (Table~\ref{tab:cf-gates}): bitwise determinism; identity at $\alpha=0$; reach; batched-versus-single agreement of the answer logits; image-free semantic mapping of every template; throughput; and fp32 recomputation of the answer logits. To pass reach, the write must change the consumer's input and answer logits without changing anything outside the consumed tokens. We declare batched-versus-single deviations above $0.25$ and neutralise them by scoring every condition of a question in one fixed batch composition. We mark templates that fail semantic mapping as ineligible and close them as such. If a checkpoint's yes/no template fails, we grade it using its eligible B-positive template (IB for Llama~3.2 Vision). The checkpoint's scorecard records the template used. The block's scorecard records every declared deviation.

## 4_setup paragraph 1 — ACCEPTED

**before**

\paragraph{Checkpoints.}
We evaluate \cfCheckpoints{} instruction-tuned checkpoints from nine families (Table~\ref{tab:cf-models}): Qwen2.5-VL 3B/7B/32B/72B, Qwen3-VL 4B/8B/32B, InternVL3.5 8B/14B/38B, Gemma~3 4B/12B/27B, MedGemma 4B/27B, Lingshu 7B/32B, LLaVA-1.5 7B/13B, LLaVA-Med~1.5 7B, and Llama~3.2~Vision 11B. Three families are medical fine-tunes (MedGemma, Lingshu, LLaVA-Med); the rest are general-purpose. The families span five vision-tower designs and three consumer designs (token insertion after a merger, token insertion after a projector, and cross-attention). Consumed-token counts range from 144 to 4{,}096 per image. Each checkpoint runs in bf16 at a pinned revision with its native processor settings and a fixed image resolution per family.

**after**

\paragraph{Checkpoints.}
We evaluate \cfCheckpoints{} instruction-tuned checkpoints across nine families (Table~\ref{tab:cf-models}): Qwen2.5-VL 3B/7B/32B/72B, Qwen3-VL 4B/8B/32B, InternVL3.5 8B/14B/38B, Gemma~3 4B/12B/27B, MedGemma 4B/27B, Lingshu 7B/32B, LLaVA-1.5 7B/13B, LLaVA-Med~1.5 7B, and Llama~3.2~Vision 11B. Three families are medical fine-tunes (MedGemma, Lingshu, LLaVA-Med). The rest are general-purpose. The checkpoints span five vision-tower designs and three consumer designs: token insertion after a merger, token insertion after a projector, and cross-attention. Consumed-token counts range from 144 to 4{,}096 per image. We run each checkpoint in bf16 at a pinned revision with its native processor settings and a fixed image resolution per family.

## 4_setup paragraph 2 — ACCEPTED

**before**

\paragraph{Datasets and concepts.}
NIH ChestX-ray14 \citep{wang2017chestxray} and CheXpert Plus provide chest radiographs with report-derived labels. We evaluate six findings on each (NIH: Effusion, Atelectasis, Pneumothorax, Cardiomegaly, Mass, Nodule; CheXpert: Effusion, Atelectasis, Pneumothorax, Cardiomegaly, Consolidation, Edema). COCO provides the natural-image control, with binary presence labels for six object categories (person, dog, car, chair, bottle, bicycle). Seeded hashes of patient (or image) identity select one fixed cohort per dataset: 20{,}000 training rows (18{,}212 on NIH, every training patient after the split), 400 calibration rows, 600 test rows, and 16 preflight rows. The training rows fit the probes and the answer direction, the calibration rows carry the readable and answerable grades, and the test rows carry every write. Every checkpoint shares these patient-disjoint splits. The chest sets use one frontal study per patient.

**after**

\paragraph{Datasets and concepts.}
NIH ChestX-ray14 \citep{wang2017chestxray} and CheXpert Plus provide chest radiographs with report-derived labels. We evaluate six findings per dataset (NIH: Effusion, Atelectasis, Pneumothorax, Cardiomegaly, Mass, Nodule; CheXpert: Effusion, Atelectasis, Pneumothorax, Cardiomegaly, Consolidation, Edema). COCO provides the natural-image control, with binary presence labels for six object categories (person, dog, car, chair, bottle, bicycle). Seeded hashes of patient (or image) identity select one fixed cohort per dataset: 20{,}000 training rows (18{,}212 on NIH, every training patient after the split), 400 calibration rows, 600 test rows, and 16 preflight rows. We fit the probes and the answer direction on training rows. We assign readable and answerable grades on calibration rows. We apply every write to test rows. All checkpoints use the same patient-disjoint splits. Both chest datasets use one frontal study per patient.

## 4_setup paragraph 3 — ACCEPTED

**before**

\paragraph{Questions and templates.}
We ask a yes/no question about each concept using six templates that cross two wordings (\emph{is there} versus \emph{does the image show}) with three answer interfaces (yes/no, A-present, B-present). The primary grade uses the \emph{is}/yes-no template. The other five provide the wording and mapping contrasts. The main write dose is $\alpha=+0.25$. The dose module adds $\alpha\in\{-0.5,-0.25,-0.1,+0.1,+0.5\}$.

**after**

\paragraph{Questions and templates.}
We ask a yes/no question about each concept with six templates that cross two wordings (\emph{is there} versus \emph{does the image show}) with three answer interfaces (yes/no, A-present, B-present). We use the \emph{is}/yes-no template for the primary grade. The other five provide wording and mapping contrasts. The main write dose is $\alpha=+0.25$. The dose module adds $\alpha\in\{-0.5,-0.25,-0.1,+0.1,+0.5\}$.

## 4_setup paragraph 4 — ACCEPTED

**before**

\paragraph{Scale.}
A block pairs one checkpoint with one dataset. Each block scores the $6\times6$ write matrix, the sham, and 119 random directions on 600 test rows (457{,}200 outcomes). NIH and COCO blocks also run the calibration, dose, refit, connector-locus, and template modules; CheXpert blocks add calibration. The campaign comprises \cfBlocks{} blocks, \cfCells{} scored concept cells, about 30 million scored answers, and roughly 3{,}000 A100-hours. \cfCheckpointsCompleteWord{} checkpoints are complete on all three datasets. Llama~3.2~Vision 11B is graded on its eligible B-positive template. We run Qwen2.5-VL-72B on NIH only. The largest checkpoint run is 72B (Appendix~\ref{app:campaign}; label handling, registration, and the released artifact are in Appendix~\ref{app:cf-labels}).

**after**

\paragraph{Scale.}
Each block pairs one checkpoint with one dataset. It scores the $6\times6$ write matrix, the sham, and 119 random directions on 600 test rows (457{,}200 outcomes). NIH and COCO blocks also run the calibration, dose, refit, connector-locus, and template modules. CheXpert blocks add calibration. The campaign comprises \cfBlocks{} blocks, \cfCells{} scored concept cells, about 30 million scored answers, and roughly 3{,}000 A100-hours. \cfCheckpointsCompleteWord{} checkpoints are complete on all three datasets. We grade Llama~3.2~Vision 11B on its eligible B-positive template. We run Qwen2.5-VL-72B on NIH only. The largest checkpoint run is 72B (Appendix~\ref{app:campaign}; label handling, registration, and the released artifact are in Appendix~\ref{app:cf-labels}).

## 5_results paragraph 2 — ACCEPTED

**before**

Table~\ref{tab:cf-main} reports every checkpoint's scores on every dataset. Figure~\ref{fig:overview} shows every concept cell across the three grades. Both chest datasets show the same pattern. On the calibration cohort, clinical concepts are readable in \cfChestReadable{} of \cfChestReadableN{} cells (\cfChestReadablePct\%): probe selectivity exceeds matched random-label controls by $0.1$--$0.3$ for most findings in most families. The models also answer there: \cfChestAnswerable{} of the \cfChestAnswerableN{} cells with a scored write matrix (\cfChestAnswerablePct\%) have a clean-answer AUROC lower bound above chance on the same 400 rows. Of these answerable cells, \cfReadAns{} are also readable. Yet only \cfChestOwned{} cells (\cfChestOwnedPct\%) are owned. In \cfChestCompetitor{} cells (\cfChestCompetitorPct\%), the verdict is \emph{stronger competitor}---a direction fitted for another finding moves the concept's answer more than its own direction does. Readability and answerability do not suffice for writing: only \cfReadAnsOwned{} of the \cfReadAns{} readable and answerable cells are owned (\cfReadAnsOwnedPct\%), compared with \cfRestOwned{} of the remaining \cfRestN{} (\cfRestOwnedPct\%).

**after**

Table~\ref{tab:cf-main} reports scores for every checkpoint on every dataset. Figure~\ref{fig:overview} shows every concept cell across the three grades. Both chest datasets show the same pattern. On the calibration cohort, clinical concepts are readable in \cfChestReadable{} of \cfChestReadableN{} cells (\cfChestReadablePct\%): probe selectivity exceeds matched random-label controls by $0.1$--$0.3$ for most findings in most families. The models also answer on this cohort: \cfChestAnswerable{} of the \cfChestAnswerableN{} cells with a scored write matrix (\cfChestAnswerablePct\%) have a clean-answer AUROC lower bound above chance on the same 400 rows. Of these answerable cells, \cfReadAns{} are also readable. Yet only \cfChestOwned{} cells (\cfChestOwnedPct\%) are owned. In \cfChestCompetitor{} cells (\cfChestCompetitorPct\%), the verdict is \emph{stronger competitor}---a direction fitted for another finding moves the concept's answer more than the concept's own direction does. Readability and answerability do not suffice for writing: only \cfReadAnsOwned{} of the \cfReadAns{} readable and answerable cells are owned (\cfReadAnsOwnedPct\%), compared with \cfRestOwned{} of the remaining \cfRestN{} (\cfRestOwnedPct\%).

## 5_results paragraph 3 — ACCEPTED

**before**

Table~\ref{tab:cf-contingency} splits chest cells by whether they meet the steering reference and by the clinical comparison verdict. The steering reference requires the concept's own write to raise its answer above the 95th percentile of the random writes and above the absolute sham. Of the \cfChestAnswerableN{} chest cells, \cfChestRefMet{} meet this reference. Among these \cfChestRefMet{} cells, \cfChestOwned{} are owned, \cfChestRefStrong{} have a stronger competitor, and \cfChestRefUnres{} are unresolved. The remaining \cfChestRefBelow{} cells do not meet the steering reference and account for \cfChestBelowStrong{} of the \cfChestCompetitor{} stronger-competitor verdicts (\cfChestBelowStrongPct\%). In these cells, the concept's own write fails to clear the random and sham references. A direction fitted for another finding moves the concept's answer more. The readable and answerable cells show the same pattern. Of the \cfReadAns{} readable and answerable cells, \cfReadAnsRefMet{} meet the steering reference. Of these, \cfReadAnsOwned{} are owned, \cfReadAnsRefStrong{} have a stronger competitor, and \cfReadAnsRefUnres{} are unresolved. Of their \cfReadAnsCompetitor{} stronger-competitor verdicts, \cfReadAnsBelowStrong{} occur in the \cfReadAnsRefBelow{} cells below the reference. On COCO, \cfCocoRefMet{} of \cfCocoOwnedN{} cells meet the steering reference. Of these, \cfCocoOwned{} are owned. Of its \cfCocoCompetitor{} stronger-competitor verdicts, \cfCocoBelowStrong{} occur below the reference.

**after**

Table~\ref{tab:cf-contingency} groups chest cells by whether they meet the steering reference and by their clinical comparison verdict. The steering reference requires the concept's own write to raise its answer above the 95th percentile of the random writes and above the absolute sham. Of the \cfChestAnswerableN{} chest cells, \cfChestRefMet{} meet this reference. Of these \cfChestRefMet{} cells, \cfChestOwned{} are owned, \cfChestRefStrong{} have a stronger competitor, and \cfChestRefUnres{} are unresolved. The remaining \cfChestRefBelow{} cells fail to meet the steering reference and account for \cfChestBelowStrong{} of the \cfChestCompetitor{} stronger-competitor verdicts (\cfChestBelowStrongPct\%). In these cells, the concept's own write fails to clear the random and sham references. A direction fitted for another finding moves the concept's answer more. Readable and answerable cells show the same pattern. Of the \cfReadAns{} readable and answerable cells, \cfReadAnsRefMet{} meet the steering reference. Of these, \cfReadAnsOwned{} are owned, \cfReadAnsRefStrong{} have a stronger competitor, and \cfReadAnsRefUnres{} are unresolved. Among the \cfReadAnsCompetitor{} stronger-competitor verdicts for readable and answerable cells, \cfReadAnsBelowStrong{} occur in the \cfReadAnsRefBelow{} cells below the reference. On COCO, \cfCocoRefMet{} of \cfCocoOwnedN{} cells meet the steering reference. Of these, \cfCocoOwned{} are owned. Of the \cfCocoCompetitor{} stronger-competitor verdicts, \cfCocoBelowStrong{} occur below the reference.

## 5_results paragraph 8 — ACCEPTED

**before**

We build on a two-model NIH study of Qwen2.5-VL-7B and LLaVA-1.5-7B (Appendix~\ref{app:seed}). We reproduce the central result in a 600-image cohort disjoint from every earlier cohort (Table~\ref{tab:cf-seed}). The Effusion write raises the Effusion answer by $\cfSeedWEff$. This effect exceeds the random 95th percentile ($\cfSeedEffRandP$) and sham ($\cfSeedEffSham$). It ranks second among the 120 compared effects. Yet the Nodule direction raises the Effusion answer by $\cfSeedWNodule$. Effusion ownership is $O_{\mathrm{Effusion}}=\cfSeedEffO$ (95\% CI $[\cfSeedEffOLo,\cfSeedEffOHi]$), matching the original $-0.057$ $[-0.069,-0.045]$. We also reproduce prompt dependence for Mass. Effusion and Cardiomegaly are the most readable chest concepts (mean selectivity $\cfSelEffusion$ and $\cfSelCardiomegaly$ over \cfChestProbeBlocks{} chest blocks). Effusion is owned in \cfEffusionOwnedBlocks{} of \cfChestBlocks{} chest blocks. Of these owned blocks, \cfEffusionOwnedSmall{} have $O_{\mathrm{Effusion}}\leq0.03$.

**after**

We build on a two-model NIH study of Qwen2.5-VL-7B and LLaVA-1.5-7B (Appendix~\ref{app:seed}). We reproduce its central result in a 600-image cohort disjoint from every earlier cohort (Table~\ref{tab:cf-seed}). The Effusion write raises the Effusion answer by $\cfSeedWEff$. This increase exceeds the random 95th percentile ($\cfSeedEffRandP$) and sham ($\cfSeedEffSham$). It ranks second among the 120 compared effects. Yet the Nodule direction raises the Effusion answer by $\cfSeedWNodule$. Effusion ownership, $O_{\mathrm{Effusion}}=\cfSeedEffO$ (95\% CI $[\cfSeedEffOLo,\cfSeedEffOHi]$), matches the original $-0.057$ $[-0.069,-0.045]$. We also reproduce prompt dependence for Mass. Effusion and Cardiomegaly are the most readable chest concepts, with mean selectivity $\cfSelEffusion$ and $\cfSelCardiomegaly$ over \cfChestProbeBlocks{} chest blocks. Effusion is owned in \cfEffusionOwnedBlocks{} of \cfChestBlocks{} chest blocks. Of these, \cfEffusionOwnedSmall{} have $O_{\mathrm{Effusion}}\leq0.03$.

## 5_results paragraph 10 — ACCEPTED

**before**

Ownership is the rarest and least stable of the three grades on the chest sets. An effect floor of $0.05$ on both $W_{q,q}$ and $O_q$ leaves \cfChestFloored{} of the \cfChestOwned{} owned chest cells (\cfChestFlooredPct\% of \cfChestAnswerableN{}), compared with \cfCocoFloored{} of the \cfCocoOwned{} owned COCO cells (\cfCocoFlooredPct\%). Under both probe refits on resampled patients, \cfRefitGradeStableNih{} of the \cfRefitGradeOwnedNih{} owned NIH cells retain ownership, compared with \cfRefitGradeStableCoco{} of \cfRefitGradeOwnedCoco{} on COCO (Table~\ref{tab:cf-refit}). Clinical writes are not selective on the chest sets. To compute the write-flow share, we take the positive part of every entry of $W$, average it over checkpoints, and divide the sum of the six own-question entries by the total. The written concept's own question receives \cfOwnShareNih\% of the positive answer change on NIH and \cfOwnShareChex\% on CheXpert, compared with \cfOwnShareCoco\% on COCO (Figure~\ref{fig:cf-w-structure}). This pattern is the attribution deficit. The model exposes the concept to a probe. The probe's direction is not a handle on the concept. Written back, it moves other findings' answers as much as its own.

**after**

Ownership is the rarest and least stable of the three grades on the chest sets. Applying an effect floor of $0.05$ to both $W_{q,q}$ and $O_q$ retains \cfChestFloored{} of the \cfChestOwned{} owned chest cells (\cfChestFlooredPct\% of \cfChestAnswerableN{}), compared with \cfCocoFloored{} of the \cfCocoOwned{} owned COCO cells (\cfCocoFlooredPct\%). Ownership survives both probe refits on resampled patients in \cfRefitGradeStableNih{} of the \cfRefitGradeOwnedNih{} owned NIH cells, compared with \cfRefitGradeStableCoco{} of \cfRefitGradeOwnedCoco{} on COCO (Table~\ref{tab:cf-refit}). Clinical writes are not selective on the chest sets. To compute the write-flow share, we take the positive part of every entry of $W$ and average it over checkpoints. We then divide the sum of the six own-question entries by the total. The written concept's own question receives \cfOwnShareNih\% of the positive answer change on NIH and \cfOwnShareChex\% on CheXpert, compared with \cfOwnShareCoco\% on COCO (Figure~\ref{fig:cf-w-structure}). This pattern is the attribution deficit. The model makes the concept readable to a probe. The probe's direction is not a handle on the concept. Written back, it moves other findings' answers as much as its own.

## 5_results paragraph 11 — ACCEPTED

**before**

Figure~\ref{fig:example} shows the pattern in individual images. On a chest radiograph, the Effusion and Nodule writes raise the same answers. The competing write raises the Effusion answer more than the Effusion write does. On a natural image, the dog write moves only the dog answer. The write matrix exposes two components of the deficit (Figure~\ref{fig:write-matrices}, Table~\ref{tab:cf-controls}). First, concept writes in the chest datasets have little effect on their own answers. The median $W_{q,q}$ is $\cfMedianWqqNih$ on NIH and $\cfMedianWqqChex$ on CheXpert, compared with $\cfMedianWqqCoco$ on COCO. Second, competing directions share the effect whenever a chest write moves an answer (Table~\ref{tab:cf-contingency}). Across the grid, the Atelectasis, Effusion, and Consolidation directions raise each other's answers by similar amounts. On COCO, the diagonal entry of $W$ dominates its row. On NIH and CheXpert, the row is flat or peaks off the diagonal. At every Qwen2.5-VL size, a competing direction raises the Effusion answer more than the Effusion direction does: Cardiomegaly at 3B, Nodule at 7B and 32B, and Atelectasis at 72B.

**after**

Figure~\ref{fig:example} shows the pattern in individual images. On a chest radiograph, the Effusion and Nodule writes raise the same answers. The competing write raises the Effusion answer more than the Effusion write does. On a natural image, the dog write moves only the dog answer. The write matrix reveals two components of the deficit (Figure~\ref{fig:write-matrices}, Table~\ref{tab:cf-controls}). First, concept writes have little effect on their own answers in the chest datasets. The median $W_{q,q}$ is $\cfMedianWqqNih$ on NIH and $\cfMedianWqqChex$ on CheXpert, compared with $\cfMedianWqqCoco$ on COCO. Second, whenever a chest write moves an answer, competing directions share the effect (Table~\ref{tab:cf-contingency}). Across the grid, the Atelectasis, Effusion, and Consolidation directions raise each other's answers by similar amounts. On COCO, the diagonal entry of $W$ dominates its row. On NIH and CheXpert, the row is flat or peaks off the diagonal. At every Qwen2.5-VL size, a competing direction raises the Effusion answer more than the Effusion direction does: Cardiomegaly at 3B, Nodule at 7B and 32B, and Atelectasis at 72B.

## 5_results paragraph 13 — ACCEPTED

**before**

The same procedure produces the opposite result on COCO (Table~\ref{tab:cf-main}, right block). Objects are owned in \cfCocoOwned{} of \cfCocoOwnedN{} cells (\cfCocoOwnedPct\%), with ownership margins of $\cfCocoMarginMin$--$\cfCocoMarginMax$ in the Qwen, Lingshu, and Gemma families. \cfCocoRefMet{} cells meet the steering reference. Only \cfCocoCompetitor{} cells have a stronger competitor. Of the \cfCocoBlocks{} checkpoints with a COCO block, \cfCocoBlocksFourPlus{} own at least four of the six objects. Dose response tracks the write. On COCO, median ownership across questions and blocks rises from $\cfDoseCocoLow$ at $\alpha=-0.1$ to $\cfDoseCocoPeak$ at $\alpha=+0.25$. It remains positive at $\alpha=+0.5$ ($\cfDoseCocoHalf$). On NIH, median ownership stays between $\cfDoseNihMin$ and $\cfDoseNihMax$ at every dose (Figure~\ref{fig:overview}c). The write, direction construction, reference family, and decision rules therefore work as intended. The deficit is specific to clinical concepts.

**after**

The same procedure yields the opposite result on COCO (Table~\ref{tab:cf-main}, right block). Objects are owned in \cfCocoOwned{} of \cfCocoOwnedN{} cells (\cfCocoOwnedPct\%). Ownership margins are $\cfCocoMarginMin$--$\cfCocoMarginMax$ in the Qwen, Lingshu, and Gemma families. \cfCocoRefMet{} cells meet the steering reference. Only \cfCocoCompetitor{} cells have a stronger competitor. \cfCocoBlocksFourPlus{} of the \cfCocoBlocks{} checkpoints with a COCO block own at least four of the six objects. Dose response tracks the write. On COCO, median ownership across questions and blocks rises from $\cfDoseCocoLow$ at $\alpha=-0.1$ to $\cfDoseCocoPeak$ at $\alpha=+0.25$. It remains positive at $\alpha=+0.5$ ($\cfDoseCocoHalf$). On NIH, median ownership remains between $\cfDoseNihMin$ and $\cfDoseNihMax$ at every dose (Figure~\ref{fig:overview}c). The write, direction construction, reference family, and decision rules therefore work as intended. The deficit is specific to clinical concepts.

## 5_results paragraph 17 — ACCEPTED

**before**

Gemma~3 shares one SigLIP tower across its 4B, 12B, and 27B checkpoints. MedGemma shares one MedSigLIP tower across 4B and 27B. LLaVA-1.5 shares one CLIP tower across 7B and 13B. We intervene primarily at the tower's final block. The design therefore holds the representation and the write fixed and changes the reader: within each family the pooled features, the fitted probes, and the lifted write vectors are identical across sizes, while the connector and the language model that consume them differ (features and write vectors bit-identical for Gemma~3 4B and 12B and for LLaVA-1.5; equal within one bf16 rounding step for Gemma~3 27B and MedGemma 27B, with raw model-space cosines above $\cfPairsTowerCosMin$ between corresponding write vectors for every concept and pair). Readability is identical across sizes by construction. Every difference in Figure~\ref{fig:readers} comes from how the connector and language model read the same written representation. The median paired difference is \cfPairsRatioMin{}--\cfPairsRatioMax{} times the refit standard deviation. It exceeds that standard deviation in \cfPairsRatioAboveOne{} of the \cfPairsRatioN{} pairs with a refit estimate (Table~\ref{tab:cf-pairs}). On NIH Effusion, the identical write on the same 600 patients produces a difference in $O_q$ of $\cfPairDeltaEffFourTwelve$ $[\cfPairDeltaEffFourTwelveLo,\cfPairDeltaEffFourTwelveHi]$ between Gemma~3 4B and 12B. The difference between 12B and 27B is $\cfPairDeltaEffTwelveTwentySeven$ $[\cfPairDeltaEffTwelveTwentySevenLo,\cfPairDeltaEffTwelveTwentySevenHi]$. In \cfPairsFiveOrSix{} of the \cfPairsN{} shared-tower pair--dataset combinations, the paired patient-bootstrap interval for $\Delta O_q$ excludes zero for five or six of the six concepts. The median $|\Delta O_q|$ is \cfPairsRatioMin{}--\cfPairsRatioMax{} times the families' refit standard deviation (up to \cfPairsRatioSingleMax{} times for single concepts). In 12B, competing directions own the chest answers outright ($O_{\mathrm{Effusion}}=\cfOGemmaTwelveEff$ on both chest sets, $O_{\mathrm{Mass}}=\cfOGemmaTwelveMass$, $O_{\mathrm{Pneumothorax}}=\cfOGemmaTwelvePneu$). In 27B, no direction moves these answers ($\cfOGemmaTwentySevenEff$, $\cfOGemmaTwentySevenMass$, $\cfOGemmaTwentySevenPneu$). In 4B, the clean answer is yes on \cfGemmaFourYesMin--\cfGemmaFourYesMax\% of chest rows. Every own write lowers its answer's logit margin more than any competing write raises it ($O^m_q$ from $\cfPairsGemmaFourOmNear$ to $\cfPairsGemmaFourOmFar$, all intervals below zero). MedGemma 4B and 27B differ in the same way (Pneumothorax $\cfOMedFourPneu$ versus $\cfOMedTwentySevenPneu$ and Edema $\cfOMedFourEdema$ versus $\cfOMedTwentySevenEdema$ on CheXpert). LLaVA-1.5 7B and 13B own no concepts on either chest set, yet own \cfLlavaSevenCoco{} and \cfLlavaThirteenCoco{} COCO objects with the same write. The chest cells owned by these readers (Gemma~3 12B: Nodule on NIH, Edema on CheXpert; MedGemma 4B: Cardiomegaly and Edema on CheXpert) carry the least evidential weight. The reader effect spans the whole matrix, regardless of which cell crosses zero.

**after**

Gemma~3 shares one SigLIP tower across its 4B, 12B, and 27B checkpoints. MedGemma shares one MedSigLIP tower across 4B and 27B. LLaVA-1.5 shares one CLIP tower across 7B and 13B. We intervene primarily at the tower's final block. This design holds the representation and write fixed while changing the reader. Within each family, pooled features, fitted probes, and lifted write vectors are identical across sizes. The connector and language model that read them differ. Features and write vectors are bit-identical for Gemma~3 4B and 12B and for LLaVA-1.5, and equal within one bf16 rounding step for Gemma~3 27B and MedGemma 27B. Raw model-space cosines between corresponding write vectors exceed $\cfPairsTowerCosMin$ for every concept and pair. Readability is identical across sizes by construction. Every difference in Figure~\ref{fig:readers} comes from how the connector and language model read the same written representation. The median paired difference is \cfPairsRatioMin{}--\cfPairsRatioMax{} times the refit standard deviation. It exceeds that standard deviation in \cfPairsRatioAboveOne{} of the \cfPairsRatioN{} pairs with a refit estimate (Table~\ref{tab:cf-pairs}). On NIH Effusion, the identical write on the same 600 patients produces a difference in $O_q$ of $\cfPairDeltaEffFourTwelve$ $[\cfPairDeltaEffFourTwelveLo,\cfPairDeltaEffFourTwelveHi]$ between Gemma~3 4B and 12B. The difference between 12B and 27B is $\cfPairDeltaEffTwelveTwentySeven$ $[\cfPairDeltaEffTwelveTwentySevenLo,\cfPairDeltaEffTwelveTwentySevenHi]$. In \cfPairsFiveOrSix{} of the \cfPairsN{} shared-tower pair--dataset combinations, the paired patient-bootstrap interval for $\Delta O_q$ excludes zero for five or six of the six concepts. The median $|\Delta O_q|$ is \cfPairsRatioMin{}--\cfPairsRatioMax{} times the families' refit standard deviation (up to \cfPairsRatioSingleMax{} times for single concepts). In 12B, competing directions own the chest answers outright ($O_{\mathrm{Effusion}}=\cfOGemmaTwelveEff$ on both chest sets, $O_{\mathrm{Mass}}=\cfOGemmaTwelveMass$, $O_{\mathrm{Pneumothorax}}=\cfOGemmaTwelvePneu$). In 27B, no direction moves these answers ($\cfOGemmaTwentySevenEff$, $\cfOGemmaTwentySevenMass$, $\cfOGemmaTwentySevenPneu$). In 4B, the clean answer is yes on \cfGemmaFourYesMin--\cfGemmaFourYesMax\% of chest rows. Every own write lowers its answer's logit margin more than any competing write raises it ($O^m_q$ from $\cfPairsGemmaFourOmNear$ to $\cfPairsGemmaFourOmFar$, all intervals below zero). MedGemma 4B and 27B differ in the same way (Pneumothorax $\cfOMedFourPneu$ versus $\cfOMedTwentySevenPneu$ and Edema $\cfOMedFourEdema$ versus $\cfOMedTwentySevenEdema$ on CheXpert). LLaVA-1.5 7B and 13B own no concepts on either chest set, yet own \cfLlavaSevenCoco{} and \cfLlavaThirteenCoco{} COCO objects with the same write. The chest cells these readers own (Gemma~3 12B: Nodule on NIH, Edema on CheXpert; MedGemma 4B: Cardiomegaly and Edema on CheXpert) carry the least evidential weight. The reader effect spans the whole matrix, regardless of which cell crosses zero.

## 5_results paragraph 20 — ACCEPTED

**before**

The same representation contains the reader's handle. For each question, we fit the model's own clean answer margin by ridge regression on the same projected, train-scaled features, using the first 3{,}000 rows of the training cohort in its fixed manifest order, which is a seeded shuffle on NIH and a hash of patient or image identity on CheXpert and COCO, and which no label or answer enters. The penalty is chosen per question by five-fold cross-validated $R^2$ over $\{0.1,1,10,100\}$: the chosen value is $1$ in \cfAnsAlphaOne{} and $10$ in \cfAnsAlphaTen{} of the \cfAnsAlphaCells{} fitted questions and $100$ in \cfAnsAlphaHundred{}. The folds split rows; on CheXpert and COCO each of the 3{,}000 rows is a distinct patient or image, and on NIH they come from \cfAnsTrainUnitsNih{} patients. We lift the fitted direction as we do a probe normal and write it at the same dose against the same reference family (Table~\ref{tab:cf-ansdir}). This answer direction $a_q$ is a handle where the label direction is not. Across the \cfAnsChestN{} chest cells in the \cfAnsChestBlocks{} scored chest blocks (Table~\ref{tab:cf-ansdir}, top), $a_q$ is owned in \cfAnsChestOwnedA{} and the label direction in \cfAnsChestOwnedLabel{}. Its write beats the strongest logistic competitor in \cfAnsChestBeats{}. Across the five held-out templates, $a_q$ remains owned in \cfAnsdirtChestKept{} of \cfAnsdirtChestKeptN{} chest and \cfAnsdirtCocoKept{} of \cfAnsdirtCocoKeptN{} COCO (cell, template) pairs (Table~\ref{tab:cf-ansdir}, bottom). Across all pairs, regardless of whether their cell is owned under the primary template, $a_q$ is owned in \cfAnsdirtChestOwnedPairs{} of \cfAnsdirtChestPairsAll{} chest and \cfAnsdirtCocoOwnedPairs{} of \cfAnsdirtCocoPairsAll{} COCO (cell, template) pairs. In Qwen2.5-VL-7B, it is owned for \cfAnsQwenOwnedNih{} of six NIH concepts (Effusion $O^a=\cfAnsQwenEffO$ with an own write of $\cfAnsQwenEffW$) and all \cfAnsQwenOwnedChex{} CheXpert concepts. In Lingshu 32B, it is owned for \cfAnsLingOwnedNih{} NIH and \cfAnsLingOwnedChex{} CheXpert concepts. In Gemma~3 12B, it is owned for \cfAnsGemOwnedNih{} NIH and \cfAnsGemOwnedChex{} CheXpert concepts. The raw model-space cosine between the answer direction and the probe normal, taken as the median over the six concepts of a block, is $\cfAnsCosQwenMin$--$\cfAnsCosQwenMax$ across the two chest blocks of Qwen2.5-VL-7B and $\cfAnsCosLingMin$--$\cfAnsCosLingMax$ across those of Lingshu 32B, compared with $\cfAnsCosCocoMin$--$\cfAnsCosCocoMax$ across the COCO blocks. The cosine whitened by the training-feature covariance in the projected, train-scaled space, taken as the median over the cells of a dataset, is $\cfValCosSigmaChestMin$--$\cfValCosSigmaChestMax$ on the two chest sets and $\cfValCosSigmaCoco$ on COCO. The two metrics are furthest apart on the CheXpert block of Lingshu 32B, where the median over its six concepts is $\cfValCosRawLingChex$ raw and $\cfValCosSigmaLingChex$ whitened. The answer direction reads the model, not the label. Over the \cfValAurocAnsChestN{} chest cells, its median AUROC against the report label is $\cfValAurocAnsChest$, against $\cfValAurocProbeChest$ for the probe; against the model's own clean answer it is $\cfValAurocAnsCleanChest$, against $\cfValAurocProbeCleanChest$ for the probe (median over the \cfValAurocAnsCleanChestN{} chest cells whose clean answers take both values; Table~\ref{tab:cf-validation}). In Qwen2.5-VL-7B on NIH, the label AUROC is $\cfValAurocAnsQwenNih$ for the answer direction and $\cfValAurocProbeQwenNih$ for the probe, and the clean-answer AUROC is $\cfValAurocAnsCleanQwenNih$ and $\cfValAurocProbeCleanQwenNih$. Ownership compares the six directions of one construction on each question's row of $W$; the column-selectivity index $S_d=W_{d,d}/\sum_q|W_{q,d}|$ of Section~\ref{sec:robust-geometry} measures the share of one direction's effect that lands on its own question. For the answer directions, the median $S_d$ is $\cfAnsColSelChest$ over the \cfAnsColSelChestN{} chest cells, against $\cfAnsColSelLabelChest$ for the label directions of the same blocks, and $\cfAnsColSelCoco$ against $\cfAnsColSelLabelCoco$ over the \cfAnsColSelCocoN{} COCO cells. On the chest sets, answer and label directions alike move the other five answers along with their own; the row comparison separates them, and the answer direction wins it in \cfAnsChestOwnedA{} of \cfAnsChestN{} cells against \cfAnsChestOwnedLabel{} for the label direction. On natural images, the readable and writable directions coincide. On chest radiographs, the model answers along a direction the probe does not read.

**after**

The same representation contains the reader's handle. For each question, we fit the model's own clean answer margin by ridge regression on the same projected, train-scaled features, using the first 3{,}000 rows of the training cohort in fixed manifest order. This order is a seeded shuffle on NIH and a hash of patient or image identity on CheXpert and COCO; neither labels nor answers enter it. We choose each question's penalty by five-fold cross-validated $R^2$ over $\{0.1,1,10,100\}$. The chosen value is $1$ in \cfAnsAlphaOne{} and $10$ in \cfAnsAlphaTen{} of the \cfAnsAlphaCells{} fitted questions and $100$ in \cfAnsAlphaHundred{}. The folds split rows. On CheXpert and COCO, each of the 3{,}000 rows is a distinct patient or image; on NIH, they come from \cfAnsTrainUnitsNih{} patients. We lift the fitted direction as we lift a probe normal and write it at the same dose against the same reference family (Table~\ref{tab:cf-ansdir}). This answer direction $a_q$ is a handle where the label direction is not. Across the \cfAnsChestN{} chest cells in the \cfAnsChestBlocks{} scored chest blocks (Table~\ref{tab:cf-ansdir}, top), $a_q$ is owned in \cfAnsChestOwnedA{} and the label direction in \cfAnsChestOwnedLabel{}. Its write beats the strongest logistic competitor in \cfAnsChestBeats{}. Across the five held-out templates, $a_q$ remains owned in \cfAnsdirtChestKept{} of \cfAnsdirtChestKeptN{} chest and \cfAnsdirtCocoKept{} of \cfAnsdirtCocoKeptN{} COCO (cell, template) pairs (Table~\ref{tab:cf-ansdir}, bottom). Across all pairs, regardless of cell ownership under the primary template, $a_q$ is owned in \cfAnsdirtChestOwnedPairs{} of \cfAnsdirtChestPairsAll{} chest and \cfAnsdirtCocoOwnedPairs{} of \cfAnsdirtCocoPairsAll{} COCO (cell, template) pairs. In Qwen2.5-VL-7B, it is owned for \cfAnsQwenOwnedNih{} of six NIH concepts (Effusion $O^a=\cfAnsQwenEffO$ with an own write of $\cfAnsQwenEffW$) and all \cfAnsQwenOwnedChex{} CheXpert concepts. In Lingshu 32B, it is owned for \cfAnsLingOwnedNih{} NIH and \cfAnsLingOwnedChex{} CheXpert concepts. In Gemma~3 12B, it is owned for \cfAnsGemOwnedNih{} NIH and \cfAnsGemOwnedChex{} CheXpert concepts. The raw model-space cosine between the answer direction and the probe normal, taken as the median over each block's six concepts, is $\cfAnsCosQwenMin$--$\cfAnsCosQwenMax$ across the two chest blocks of Qwen2.5-VL-7B and $\cfAnsCosLingMin$--$\cfAnsCosLingMax$ across those of Lingshu 32B, compared with $\cfAnsCosCocoMin$--$\cfAnsCosCocoMax$ across the COCO blocks. The cosine whitened by the training-feature covariance in the projected, train-scaled space, taken as the median over each dataset's cells, is $\cfValCosSigmaChestMin$--$\cfValCosSigmaChestMax$ on the two chest sets and $\cfValCosSigmaCoco$ on COCO. The two metrics differ most on the CheXpert block of Lingshu 32B, where the median over its six concepts is $\cfValCosRawLingChex$ raw and $\cfValCosSigmaLingChex$ whitened. The answer direction reads the model, not the label. Over the \cfValAurocAnsChestN{} chest cells, its median AUROC against the report label is $\cfValAurocAnsChest$, compared with $\cfValAurocProbeChest$ for the probe. Against the model's own clean answer, its median AUROC is $\cfValAurocAnsCleanChest$, compared with $\cfValAurocProbeCleanChest$ for the probe (median over the \cfValAurocAnsCleanChestN{} chest cells whose clean answers take both values; Table~\ref{tab:cf-validation}). In Qwen2.5-VL-7B on NIH, the label AUROC is $\cfValAurocAnsQwenNih$ for the answer direction and $\cfValAurocProbeQwenNih$ for the probe. The clean-answer AUROC is $\cfValAurocAnsCleanQwenNih$ and $\cfValAurocProbeCleanQwenNih$, respectively. Ownership compares the six directions of one construction on each question's row of $W$. The column-selectivity index $S_d=W_{d,d}/\sum_q|W_{q,d}|$ of Section~\ref{sec:robust-geometry} measures the share of one direction's effect that lands on its own question. For the answer directions, the median $S_d$ is $\cfAnsColSelChest$ over the \cfAnsColSelChestN{} chest cells, compared with $\cfAnsColSelLabelChest$ for the label directions of the same blocks. The corresponding medians are $\cfAnsColSelCoco$ and $\cfAnsColSelLabelCoco$ over the \cfAnsColSelCocoN{} COCO cells. On the chest sets, answer and label directions alike move the other five answers along with their own. The row comparison separates them: the answer direction wins in \cfAnsChestOwnedA{} of \cfAnsChestN{} cells against \cfAnsChestOwnedLabel{} for the label direction. On natural images, the readable and writable directions coincide. On chest radiographs, the model answers along a direction the probe does not read.

## 5_results paragraph 22 — ACCEPTED

**before**

Ownership on the chest sets is concentrated among a few readers and varies by concept and dataset (Figure~\ref{fig:ladders}; Table~\ref{tab:cf-models}). Qwen2.5-VL owns no concepts at 3B or 7B. At 32B, it owns Cardiomegaly. At 72B, it owns Atelectasis ($O=\cfOQwenSeventyTwoAtel$), Cardiomegaly, and Mass. Effusion is anti-owned at every size. Lingshu, the medical Qwen2.5-VL derivative, owns three CheXpert concepts at 7B. At 32B, it owns five, the highest clinical ownership in the grid. InternVL3.5 answers every NIH question at every size. Its writes remain within $\cfInternvlWqqMax$ of zero on the chest sets. LLaVA-1.5 reads the chest concepts. It answers none above chance. It owns none.

**after**

On the chest sets, ownership is concentrated among a few readers and varies by concept and dataset (Figure~\ref{fig:ladders}; Table~\ref{tab:cf-models}). Qwen2.5-VL owns no concepts at 3B or 7B. It owns Cardiomegaly at 32B. At 72B, it owns Atelectasis ($O=\cfOQwenSeventyTwoAtel$), Cardiomegaly, and Mass. Effusion is anti-owned at every size. Lingshu, the medical Qwen2.5-VL derivative, owns three CheXpert concepts at 7B. It owns five at 32B, the highest clinical ownership in the grid. InternVL3.5 answers every NIH question at every size. Its writes remain within $\cfInternvlWqqMax$ of zero on the chest sets. LLaVA-1.5 reads the chest concepts. It answers none above chance. It owns none.

## 5_results paragraph 24 — ACCEPTED

**before**

The reference family and additional controls behave as designed across the grid (Figure~\ref{fig:overview}d; Table~\ref{tab:cf-controls}). Owned concept writes rank 1--6 among the 120 compared effects. The concept write typically ranks in the top decile on the chest sets.

**after**

Across the grid, the reference family and additional controls behave as designed (Figure~\ref{fig:overview}d; Table~\ref{tab:cf-controls}). Owned concept writes rank 1--6 among the 120 compared effects. On the chest sets, the concept write typically ranks in the top decile.

## 5_results paragraph 26 — ACCEPTED

**before**

\paragraph{Geometry, scale, saturation, refits, numerics.} No measurement alternative explains the deficit (Tables~\ref{tab:cf-geometry}--\ref{tab:cf-validation}; Appendix~\ref{app:cf-robustness}). The strongest competitor matches the most similar direction or the most co-occurring label at chance rate. Logit-scale verdicts agree in \cfScaleAgree{} of \cfScaleAgreeN{} cells. COCO blocks are more saturated than chest blocks. Restricting each cell to unsaturated rows preserves the full owned grade in \cfScaleUnsatGradeKept{} of \cfScaleUnsatGradeN{} owned chest cells, with the steering reference recomputed on those rows and the simultaneous max-$T$ advantage evaluated over the same six-direction family. Refits change $O_q$ by a median of \cfScaleRefitMedian{}. The column-selectivity index $S_d=W_{d,d}/\sum_q|W_{q,d}|$ divides a direction's signed effect on its own question by its summed absolute effect on the six questions. Its median over cells is \cfValColSelNih{} on NIH and \cfValColSelChex{} on CheXpert, versus \cfValColSelCoco{} on COCO (Table~\ref{tab:cf-validation}). $S_d$ keeps negative effects and weights every cell equally, so it differs from the write-flow share of Section~\ref{sec:deficit}, which keeps only positive effects and pools them over checkpoints. On CheXpert, rows with a known label preserve the sign of $O_q$ in \cfValKnownSignAgree{} of \cfValKnownN{} cells. Re-grading the same fitted directions on \cfValidRows{} CheXpert films whose labels are a radiologist consensus, and which share no patient with any campaign cohort, reproduces the owned grade in \cfValidOwnedAgree{} of \cfValidCells{} cells (Table~\ref{tab:cf-valid}); the directions in both grades are fitted on report-derived labels. Refitting the six directions on the radiologist labels themselves changes the label source of the fit. Across the \cfValidfitBlocks{} CheXpert blocks with that refit, the expert-label and report-label directions have a median raw model-space cosine of \cfValidfitCosRaw{} and a median whitened cosine of \cfValidfitCosWhitened{}; the expert-label refit reads the radiologist labels at a median cross-fitted AUROC of \cfValidfitAurocExpert{}, against \cfValidfitAurocReportDir{} for the report-label direction on the same films; and written on the test rows under the same references, the expert-label directions are owned in \cfValidfitOwnedExpert{} of \cfValidfitCells{} cells, the report-label directions in \cfValidfitOwnedReport{} (Appendix~\ref{app:cf-robustness}). Rescoring in fp32 and at batch size one changes $W$ by at most \cfPrecisionMaxDW{} and alters \cfPrecisionGradeChanges{} of \cfPrecisionGradeCells{} grades (Table~\ref{tab:cf-precision}).

**after**

\paragraph{Geometry, scale, saturation, refits, numerics.} No measurement alternative explains the deficit (Tables~\ref{tab:cf-geometry}--\ref{tab:cf-validation}; Appendix~\ref{app:cf-robustness}). The strongest competitor matches the most similar direction or the most co-occurring label at chance rate. Logit-scale verdicts agree in \cfScaleAgree{} of \cfScaleAgreeN{} cells. COCO blocks are more saturated than chest blocks. Restricting each cell to unsaturated rows preserves the full owned grade in \cfScaleUnsatGradeKept{} of \cfScaleUnsatGradeN{} owned chest cells. We recompute the steering reference on those rows. We evaluate the simultaneous max-$T$ advantage over the same six-direction family. Refits change $O_q$ by a median of \cfScaleRefitMedian{}. The column-selectivity index $S_d=W_{d,d}/\sum_q|W_{q,d}|$ divides a direction's signed effect on its own question by its summed absolute effect on the six questions. Its median over cells is \cfValColSelNih{} on NIH and \cfValColSelChex{} on CheXpert, versus \cfValColSelCoco{} on COCO (Table~\ref{tab:cf-validation}). $S_d$ keeps negative effects and weights every cell equally, so it differs from the write-flow share of Section~\ref{sec:deficit}, which keeps only positive effects and pools them over checkpoints. On CheXpert, rows with a known label preserve the sign of $O_q$ in \cfValKnownSignAgree{} of \cfValKnownN{} cells. Re-grading the same fitted directions on \cfValidRows{} CheXpert films labelled by radiologist consensus reproduces the owned grade in \cfValidOwnedAgree{} of \cfValidCells{} cells (Table~\ref{tab:cf-valid}). These films share no patient with any campaign cohort. Both grades use directions fitted on report-derived labels. Refitting the six directions on the radiologist labels themselves changes the fit's label source. Across the \cfValidfitBlocks{} CheXpert blocks with that refit, the expert-label and report-label directions have a median raw model-space cosine of \cfValidfitCosRaw{} and a median whitened cosine of \cfValidfitCosWhitened{}. The expert-label refit reads the radiologist labels at a median cross-fitted AUROC of \cfValidfitAurocExpert{}, against \cfValidfitAurocReportDir{} for the report-label direction on the same films. Written on the test rows under the same references, the expert-label directions are owned in \cfValidfitOwnedExpert{} of \cfValidfitCells{} cells, the report-label directions in \cfValidfitOwnedReport{} (Appendix~\ref{app:cf-robustness}). Rescoring in fp32 and at batch size one changes $W$ by at most \cfPrecisionMaxDW{} and alters \cfPrecisionGradeChanges{} of \cfPrecisionGradeCells{} grades (Table~\ref{tab:cf-precision}).

## 5_results paragraph 27 — ACCEPTED

**before**

\paragraph{Alternative direction constructions.} The deficit is not specific to the logistic normal. Four alternative directions use the same projected, train-scaled features and write protocol (Table~\ref{tab:cf-altdir}): the difference of class means, the Haufe pattern $\Sigma\boldsymbol\beta_c$ \citep{haufe2014interpretation}, the normal orthogonalised against the other five normals, and the normal refitted on features residualised against the other five labels. Across the \cfAltdirBlocks{} scored blocks (\cfAltdirBlocksPerDs{} per dataset), each construction owns \cfAltdirChestPctMin{}--\cfAltdirChestPctMax\% of chest cells and \cfAltdirCocoPctMin{}--\cfAltdirCocoPctMax\% of COCO cells. Across those blocks, the absolute raw model-space cosine between the difference-of-means or pattern direction and the logistic normal, taken as the median over the six concepts of a block, ranges from $\cfAltdirCosMin$ to $\cfAltdirCosMax$. Each of these two directions owns more NIH cells and fewer COCO cells than the logistic normal. Lifted as displacements, these directions respectively own \cfAltdirdChestDom{} and \cfAltdirdChestPattern{} of \cfAltdirdChestN{} chest cells and \cfAltdirdCocoDom{} and \cfAltdirdCocoPattern{} of \cfAltdirdCocoN{} COCO cells (Table~\ref{tab:cf-altdir}). Every construction preserves the chest-to-COCO gap. At least one construction owns \cfAltdirNihAny{} of \cfAltdirNihN{} cells on NIH and \cfAltdirChexAny{} of \cfAltdirChexN{} on CheXpert, compared with \cfAltdirCocoAny{} of \cfAltdirCocoN{} on COCO. Extending the clinical family to every additional dataset label with enough support retains \cfExtcompRetained{} of the \cfExtcompOwned{} owned cells in the \cfExtcompBlocks{} scored blocks (Table~\ref{tab:cf-altdir}). Projection, sex, and age join the findings in one nine-direction family. These three are non-clinical properties of the same radiographs. Their probes read them at least as well as the clinical probes read the findings. Median probe AUROC is \cfAttrAurocMin{}--\cfAttrAurocMax{} per attribute, compared with \cfAttrClinAuroc{} for findings in the same blocks. All \cfAttrReadable{} of \cfAttrChestAttrN{} attribute cells are readable. The attributes own \cfAttrChestAttrOwned{} of \cfAttrChestAttrN{} cells; findings own \cfAttrChestClinOwned{} of \cfAttrChestClinN{}. On NIH, ownership is \cfAttrNihAttrOwned{}/\cfAttrNihAttrN{} for attributes and \cfAttrNihClinOwned{}/\cfAttrNihClinN{} for findings (Table~\ref{tab:cf-attr}). We weight writes along the logistic directions by the softmax of token probe scores or restrict writes to the top quarter of tokens at four times the weight (twice the Frobenius norm of the uniform write). Across the \cfTokenwChestBlocks{} chest blocks with these writes, the softmax write owns \cfTokenwSoftChestOwned{} of \cfTokenwChestCells{} cells and the top-quarter write \cfTokenwTopqChestOwned{}, against \cfTokenwUniformChestOwned{} for the uniform write; on COCO, they own \cfTokenwSoftCocoOwned{} and \cfTokenwTopqCocoOwned{} of \cfTokenwCocoCells{} cells (Table~\ref{tab:cf-altdir}). Concentrating the write where the probe reads does not create a clinical handle.

**after**

\paragraph{Alternative direction constructions.} The deficit persists beyond the logistic normal. We test four alternative directions using the same projected, train-scaled features and write protocol (Table~\ref{tab:cf-altdir}): the difference of class means, the Haufe pattern $\Sigma\boldsymbol\beta_c$ \citep{haufe2014interpretation}, the normal orthogonalised against the other five normals, and the normal refitted on features residualised against the other five labels. Across the \cfAltdirBlocks{} scored blocks (\cfAltdirBlocksPerDs{} per dataset), each construction owns \cfAltdirChestPctMin{}--\cfAltdirChestPctMax\% of chest cells and \cfAltdirCocoPctMin{}--\cfAltdirCocoPctMax\% of COCO cells. We measure the absolute raw model-space cosine of the difference-of-means and pattern directions with the logistic normal. The median over the six concepts in each block ranges from $\cfAltdirCosMin$ to $\cfAltdirCosMax$. Each of these two directions owns more NIH cells and fewer COCO cells than the logistic normal. Lifted as displacements, these directions respectively own \cfAltdirdChestDom{} and \cfAltdirdChestPattern{} of \cfAltdirdChestN{} chest cells and \cfAltdirdCocoDom{} and \cfAltdirdCocoPattern{} of \cfAltdirdCocoN{} COCO cells (Table~\ref{tab:cf-altdir}). Every construction preserves the chest-to-COCO gap. At least one construction owns \cfAltdirNihAny{} of \cfAltdirNihN{} cells on NIH and \cfAltdirChexAny{} of \cfAltdirChexN{} on CheXpert, compared with \cfAltdirCocoAny{} of \cfAltdirCocoN{} on COCO. Extending the clinical family to every additional dataset label with enough support retains \cfExtcompRetained{} of the \cfExtcompOwned{} owned cells in the \cfExtcompBlocks{} scored blocks (Table~\ref{tab:cf-altdir}). We combine projection, sex, age, and the findings in one nine-direction family. The three attributes are non-clinical properties of the same radiographs. Their probes read them at least as well as the clinical probes read the findings. Median probe AUROC per attribute is \cfAttrAurocMin{}--\cfAttrAurocMax{}, compared with \cfAttrClinAuroc{} for findings in the same blocks. All \cfAttrReadable{} of \cfAttrChestAttrN{} attribute cells are readable. The attributes own \cfAttrChestAttrOwned{} of \cfAttrChestAttrN{} cells; findings own \cfAttrChestClinOwned{} of \cfAttrChestClinN{}. On NIH, ownership is \cfAttrNihAttrOwned{}/\cfAttrNihAttrN{} for attributes and \cfAttrNihClinOwned{}/\cfAttrNihClinN{} for findings (Table~\ref{tab:cf-attr}). We weight writes along the logistic directions by the softmax of token probe scores or restrict them to the top quarter of tokens at four times the weight (twice the Frobenius norm of the uniform write). Across the \cfTokenwChestBlocks{} chest blocks with these writes, the softmax write owns \cfTokenwSoftChestOwned{} of \cfTokenwChestCells{} cells, the top-quarter write owns \cfTokenwTopqChestOwned{}, and the uniform write owns \cfTokenwUniformChestOwned{}. On COCO, the softmax and top-quarter writes own \cfTokenwSoftCocoOwned{} and \cfTokenwTopqCocoOwned{} of \cfTokenwCocoCells{} cells (Table~\ref{tab:cf-altdir}). Concentrating the write where the probe reads does not create a clinical handle.

## 6_implications paragraph 1 — ACCEPTED

**before**

\paragraph{For steering, model building, and clinical evaluation.}
A probe direction that reads a clinical concept is not a handle on it. Probe accuracy and answer accuracy, the two quantities most often reported for medical VLMs, do not establish whether a handle exists. Both are high in this grid, yet ownership is \cfChestOwnedPct\%. Beating random and sham controls shows that a steering write reaches the answer, not that the label names the direction. Of the \cfChestRefMet{} clinical cells whose own write clears those controls, \cfChestRefStrong{} lose to a direction fitted for another finding at the same locus, dose, and patients, and \cfChestRefUnres{} are unresolved, leaving \cfChestOwned{} owned. The matched-competitor test is cheap once competitors' directions exist. Studies using probe normals as interventions should report the write matrix $W$, compare ownership $O_q$ across layers, doses, and models, and pair every clinical write with a natural-image control. The same write vector produces different ownership in different language models. A vision tower that exposes a finding linearly therefore does not make its direction a handle.

**after**

\paragraph{For steering, model building, and clinical evaluation.}
A probe direction that reads a clinical concept is not a handle on it. Probe accuracy and answer accuracy, the two quantities most often reported for medical VLMs, do not establish whether a handle exists. Both are high in this grid, yet ownership is \cfChestOwnedPct\%. Beating random and sham controls shows that a steering write reaches the answer, not that the direction belongs to the labelled finding. Of the \cfChestRefMet{} clinical cells whose own write clears those controls, \cfChestRefStrong{} lose to a direction fitted for another finding at the same locus and dose in the same patients. A further \cfChestRefUnres{} are unresolved, leaving \cfChestOwned{} owned. Once competitors' directions exist, the matched-competitor test is cheap. Studies using probe normals as interventions should report the write matrix $W$, compare ownership $O_q$ across layers, doses, and models, and pair every clinical write with a natural-image control. The same write vector produces different ownership across language models. A vision tower that exposes a finding linearly therefore does not make its direction a handle.

## app_campaign paragraph 3 — ACCEPTED

**before**

\paragraph{Labels.} We use the report-derived labels supplied with each dataset (NIH ChestX-ray14 finding labels; the CheXpert labeler output distributed with CheXpert Plus; COCO instance annotations). A row is positive for label 1, negative for label 0, and \emph{unknown} when the labeler output is uncertain or blank. We exclude unknown rows from probe fitting, selectivity, and answer AUROC. We write and score them like all other rows. Fixed manifests drawn with recorded seeds define the cohorts. Training and test cohorts are patient-disjoint. Every cohort is disjoint from every seed-study cohort. Table~\ref{tab:cf-prevalence} gives the per-concept counts of the three scored cohorts: the training cohort, which fits the scaler, the probes, and the answer direction; the 400-row calibration cohort, which carries the readable and answerable grades; and the 600-row test cohort, which carries every write. A second CheXpert cohort contains \cfValidRows{} frontal films from the CheXpert validation set, one per patient, with the radiologists' consensus labels. It has zero patient overlap with any campaign cohort (Table~\ref{tab:cf-valid}).

**after**

\paragraph{Labels.} We use the report-derived labels supplied with each dataset: NIH ChestX-ray14 finding labels, the CheXpert labeler output distributed with CheXpert Plus, and COCO instance annotations. A row is positive for label 1, negative for label 0, and \emph{unknown} when the labeler output is uncertain or blank. We exclude unknown rows from probe fitting, selectivity, and answer AUROC. We write and score these rows like all others. Fixed manifests drawn with recorded seeds define the cohorts. Training and test cohorts are patient-disjoint. Every cohort is disjoint from every seed-study cohort. Table~\ref{tab:cf-prevalence} reports per-concept counts for the three scored cohorts: the training cohort for fitting the scaler, probes, and answer direction; the 400-row calibration cohort for the readable and answerable grades; and the 600-row test cohort for every write. A second CheXpert cohort contains \cfValidRows{} frontal films from the CheXpert validation set, one per patient, with the radiologists' consensus labels. It has zero patient overlap with any campaign cohort (Table~\ref{tab:cf-valid}).

## app_campaign paragraph 4 — ACCEPTED

**before**

\paragraph{Registration.} We froze the protocol before scoring any grid block. The loci, projection and probe settings, dose, reference family, six templates, module set, seven gates, and three decision rules match the registered protocol file and the code version it names. We registered the seed study's crossover and confirmation analyses before running them (Appendix~\ref{app:seed}). One analysis pass over the packaged outcomes produces everything reported per block. After completing the grid, we added the leaderboard, write-flow share, and per-image examples as descriptive summaries of these outputs.

**after**

\paragraph{Registration.} We froze the protocol before scoring any grid block. The loci, projection and probe settings, dose, reference family, six templates, module set, seven gates, and three decision rules match the registered protocol file and the code version it names. We registered the seed study's crossover and confirmation analyses before running them (Appendix~\ref{app:seed}). One analysis pass over the packaged outcomes produces all results reported per block. After completing the grid, we added the leaderboard, write-flow share, and per-image examples to summarise these outputs descriptively.

## app_campaign paragraph 5 — ACCEPTED

**before**

\paragraph{Artifact.} The release includes evaluation code and adapters, the registered protocol file, cohort manifests with their seeds, fitted directions, per-image outcomes for every module (write, dose, refit, locus, and template rows), scorecards and coverage files for every block, and rendered prompts. Adding a new checkpoint requires one adapter that identifies the block it consumes and passes the gates.

**after**

\paragraph{Artifact.} The release includes evaluation code and adapters, the registered protocol file, cohort manifests with their seeds, and fitted directions. It also includes per-image outcomes for every module (write, dose, refit, locus, and template rows), scorecards and coverage files for every block, and rendered prompts. Each new checkpoint requires one adapter that identifies the block it consumes and passes the gates.

## app_campaign paragraph 9 — ACCEPTED

**before**

For Llama~3.2 Vision, the projector also reads five intermediate local-layer outputs that bypass the final global block. LLaVA computes the final CLIP block but consumes the penultimate block instead. For every checkpoint, Gate (C) verifies that the write reaches the consumer and the answer logits while changing nothing outside the consumed tokens.

**after**

The Llama~3.2 Vision projector also reads five intermediate local-layer outputs that bypass the final global block. LLaVA computes the final CLIP block but consumes the penultimate block. For every checkpoint, Gate (C) verifies that the write reaches the consumer and the answer logits without changing anything outside the consumed tokens.

## app_campaign paragraph 11 — ACCEPTED

**before**

All \cfWriteBlocks{} completed blocks pass checks (A), (B), (C), and (G) with zero determinism error, zero change at $\alpha=0$, and fp32-versus-model logit differences below \cfGateFpMax{}. In check (D), the Qwen3-VL, Gemma~3, MedGemma, InternVL, and Llama families exceed the 0.25-logit tolerance (up to \cfGateBatchMax{} logits for Gemma~3 12B). We record these as declared deviations. Fixed batch composition neutralises them. Check (E) fails on all datasets for the A/B templates for LLaVA-1.5-7B and LLaVA-Med, the B-mapped templates for Qwen2.5-VL-3B and LLaVA-1.5-13B, and the yes/no and A-mapped templates for Llama~3.2 Vision 11B. We record the affected cells as ineligible. Two checkpoints answer ``yes'' to nearly every chest question (Gemma~3 4B: clean $P(\mathrm{yes})>0.99$ on \cfGemmaFourYesMin--\cfGemmaFourYesMax\% of rows). The answerability grade records this behaviour. Three checkpoints (LLaVA-1.5 7B/13B, LLaVA-Med) answer at chance.

**after**

All \cfWriteBlocks{} completed blocks pass checks (A), (B), (C), and (G) with zero determinism error, zero change at $\alpha=0$, and fp32-versus-model logit differences below \cfGateFpMax{}. In check (D), the Qwen3-VL, Gemma~3, MedGemma, InternVL, and Llama families exceed the 0.25-logit tolerance (up to \cfGateBatchMax{} logits for Gemma~3 12B). We record these as declared deviations. Fixed batch composition neutralises them. Across all datasets, check (E) fails for the A/B templates for LLaVA-1.5-7B and LLaVA-Med, the B-mapped templates for Qwen2.5-VL-3B and LLaVA-1.5-13B, and the yes/no and A-mapped templates for Llama~3.2 Vision 11B. We record affected cells as ineligible. Two checkpoints answer ``yes'' to nearly every chest question (Gemma~3 4B: clean $P(\mathrm{yes})>0.99$ on \cfGemmaFourYesMin--\cfGemmaFourYesMax\% of rows). The answerability grade records this behaviour. Three checkpoints (LLaVA-1.5 7B/13B, LLaVA-Med) answer at chance.

## app_campaign paragraph 13 — ACCEPTED

**before**

\cfCheckpointsCompleteWord{} checkpoints are complete on all three datasets (Table~\ref{tab:cf-coverage}). Coverage includes all modules on NIH and COCO---the write matrix, calibration, the five additional templates, the dose sweep, the two refits, and the connector locus---and the write matrix and calibration on CheXpert. We grade Llama~3.2 Vision 11B on its eligible B-positive template IB. Qwen2.5-VL-72B is complete on NIH. The compute budget prevented runs of the 72B COCO/CheXpert blocks and Llama~3.2 Vision 90B. InternVL3.5-38B is complete on all three datasets. The campaign used about 3,000 A100-hours. A chest block for a 7B checkpoint takes about ten GPU-hours.

**after**

\cfCheckpointsCompleteWord{} checkpoints are complete on all three datasets (Table~\ref{tab:cf-coverage}). NIH and COCO coverage includes all modules: the write matrix, calibration, the five additional templates, the dose sweep, the two refits, and the connector locus. CheXpert coverage includes the write matrix and calibration. We grade Llama~3.2 Vision 11B on its eligible B-positive template IB. Qwen2.5-VL-72B is complete on NIH. InternVL3.5-38B is complete on all three datasets. The compute budget prevented runs of the 72B COCO/CheXpert blocks and Llama~3.2 Vision 90B. The campaign used about 3,000 A100-hours. A chest block for a 7B checkpoint takes about ten GPU-hours.

## app_campaign paragraph 16 — ACCEPTED

**before**

Tables~\ref{tab:cf-own-nih}--\ref{tab:cf-own-coco} report the ownership contrast for every concept cell. Figure~\ref{fig:cf-w-structure} summarises the structure of the underlying write matrices. Figure~\ref{fig:cf-examples} shows grades for individual images.

**after**

Tables~\ref{tab:cf-own-nih}--\ref{tab:cf-own-coco} report the ownership contrast for each concept cell. Figure~\ref{fig:cf-w-structure} summarises the structure of the underlying write matrices. Figure~\ref{fig:cf-examples} shows grades for individual images.

## app_campaign paragraph 22 — ACCEPTED

**before**

The projected features are $x=R^{\top}\hvec$, where $R\in\R^{D\times512}$ is the fixed Gaussian projection. Two transformations map vectors from the projected, train-scaled space back to the consumed space. A classifier coefficient $\boldsymbol\beta$ lifts as $\hat\wvec\propto R\,\mathrm{diag}(1/\boldsymbol s)\boldsymbol\beta$, so the written direction scores the concept as the probe does (Equation~\ref{eq:lift}). A displacement $\boldsymbol u$ in that space lifts through its minimum-norm preimage as $\hat\wvec\propto R(R^{\top}R)^{-1}\mathrm{diag}(\boldsymbol s)\boldsymbol u$. This reproduces the displacement exactly: $R^{\top}\hat\wvec\propto\mathrm{diag}(\boldsymbol s)\boldsymbol u$. The lifts differ because $R^{\top}R$ is not a multiple of the identity. Measurements on every ALTDIRD block show that $R^{\top}R/D$ is full rank, with eigenvalues $\cfAltdirdEigMin$--$\cfAltdirdEigMax$ and condition number \cfAltdirdCondMin{}--\cfAltdirdCondMax{}. We compute the difference-of-means and pattern vectors in the projected, train-scaled space that the logistic normal uses and report both lifts. The coefficient lift is the family the ALTDIR module scores. The displacement lift is the geometrically matched comparison for those two families: a difference of class means and a Haufe pattern are displacements in the projected space rather than classifier coefficients. The orthogonalised normal removes the span of the other five normals from the logistic normal, and the residualised normal is refitted on features residualised against the other five labels.

**after**

The projected features are $x=R^{\top}\hvec$, where $R\in\R^{D\times512}$ is the fixed Gaussian projection. Two transformations map vectors from the projected, train-scaled space back to the consumed space. A classifier coefficient $\boldsymbol\beta$ lifts as $\hat\wvec\propto R\,\mathrm{diag}(1/\boldsymbol s)\boldsymbol\beta$. The written direction thus scores the concept as the probe does (Equation~\ref{eq:lift}). A displacement $\boldsymbol u$ in that space lifts via its minimum-norm preimage as $\hat\wvec\propto R(R^{\top}R)^{-1}\mathrm{diag}(\boldsymbol s)\boldsymbol u$. This reproduces the displacement exactly: $R^{\top}\hat\wvec\propto\mathrm{diag}(\boldsymbol s)\boldsymbol u$. The lifts differ because $R^{\top}R$ is not a multiple of the identity. Measurements on every ALTDIRD block show that $R^{\top}R/D$ is full rank, with eigenvalues $\cfAltdirdEigMin$--$\cfAltdirdEigMax$ and condition number \cfAltdirdCondMin{}--\cfAltdirdCondMax{}. We compute the difference-of-means and pattern vectors in the same projected, train-scaled space as the logistic normal and report both lifts. The ALTDIR module scores the coefficient-lift family. The displacement lift provides the geometrically matched comparison for these two families: a difference of class means and a Haufe pattern are displacements in the projected space rather than classifier coefficients. We form the orthogonalised normal by removing the span of the other five normals from the logistic normal. We refit the residualised normal on features residualised against the other five labels.

## app_campaign paragraph 23 — ACCEPTED

**before**

\paragraph{Alternative directions, weights, and competitor families.} The token-weighted variants multiply the per-token dose $\alpha\lVert\hvec_{it}\rVert$ by a weight $w_t$ with mean one over the consumed tokens. The softmax variant weights each token by the softmax of its probe score and preserves the L1 dose of the uniform write. The top-quarter variant ($w_t=4$ on the 25\% highest-scoring tokens) has twice the uniform write's Frobenius norm. The extended competitor family adds a direction, fitted and written like the protocol normals, for every other dataset label with at least 100 known positives and 100 known negatives (NIH: Consolidation, Edema, Infiltration; CheXpert: Enlarged Cardiomediastinum, Fracture, Lung Lesion, Lung Opacity, Pneumonia, Support Devices); a cell is owned under it when its write meets the steering reference and holds the simultaneous advantage over the five protocol competitors and every extra direction. Table~\ref{tab:cf-altdir} reports every construction, lift, token weighting, and competitor family per dataset, with the logistic normal of the protocol on the same blocks in each panel.

**after**

\paragraph{Alternative directions, weights, and competitor families.} Token-weighted variants multiply the per-token dose $\alpha\lVert\hvec_{it}\rVert$ by a weight $w_t$ with mean one over the consumed tokens. The softmax variant weights tokens by the softmax of their probe scores and preserves the L1 dose of the uniform write. The top-quarter variant ($w_t=4$ on the 25\% highest-scoring tokens) has twice the uniform write's Frobenius norm. The extended competitor family adds a direction for every other dataset label with at least 100 known positives and 100 known negatives (NIH: Consolidation, Edema, Infiltration; CheXpert: Enlarged Cardiomediastinum, Fracture, Lung Lesion, Lung Opacity, Pneumonia, Support Devices). Each added direction is fitted and written like the protocol normals. A cell is owned under this family when its write meets the steering reference and holds the simultaneous advantage over the five protocol competitors and every extra direction. Table~\ref{tab:cf-altdir} reports every construction, lift, token weighting, and competitor family per dataset, with the protocol's logistic normal on the same blocks in each panel.

## app_campaign paragraph 24 — ACCEPTED

**before**

\paragraph{Non-clinical attributes.} The attribute directions use three labels from each dataset's own metadata: the view field (anteroposterior or portable against posteroanterior), the recorded sex, and age at least 60. We fit these directions using the same images, features, projection, and probe settings as the clinical probes. We then lift and write them with the six clinical directions as one nine-direction family. Attribute questions have no random family, so the sham is their sole steering reference. Their probes reach a median AUROC of \cfAttrAurocMin{}--\cfAttrAurocMax{} against the attribute label and \cfAttrAnswerAurocMin{}--\cfAttrAnswerAurocMax{} against the model's own clean answer. Taken per attribute--finding pair as the median over blocks, the largest absolute raw model-space cosine between an attribute direction and a clinical normal is \cfAttrCosMax{}. Across the \cfAttrBlocks{} blocks with the module, the attributes are owned in \cfAttrChestAttrOwned{} of \cfAttrChestAttrN{} cells and the findings in the same family in \cfAttrChestClinOwned{} of \cfAttrChestClinN{}. The corresponding counts are \cfAttrChexAttrOwned{}/\cfAttrChexAttrN{} against \cfAttrChexClinOwned{}/\cfAttrChexClinN{} on CheXpert and \cfAttrNihAttrOwned{}/\cfAttrNihAttrN{} against \cfAttrNihClinOwned{}/\cfAttrNihClinN{} on NIH.

**after**

\paragraph{Non-clinical attributes.} The attribute directions use three labels from each dataset's own metadata: the view field (anteroposterior or portable against posteroanterior), recorded sex, and age at least 60. We fit these directions using the same images, features, projection, and probe settings as the clinical probes. We then lift and write them together with the six clinical directions as one nine-direction family. Attribute questions have no random family, so the sham is their sole steering reference. Their probes reach a median AUROC of \cfAttrAurocMin{}--\cfAttrAurocMax{} against the attribute label and \cfAttrAnswerAurocMin{}--\cfAttrAnswerAurocMax{} against the model's own clean answer. The largest absolute raw model-space cosine between an attribute direction and a clinical normal is \cfAttrCosMax{}, with each attribute--finding pair summarised by its median over blocks. Across the \cfAttrBlocks{} blocks with the module, ownership counts are \cfAttrChestAttrOwned{} of \cfAttrChestAttrN{} cells for attributes and \cfAttrChestClinOwned{} of \cfAttrChestClinN{} for findings in the same family. The corresponding attribute and finding counts are \cfAttrChexAttrOwned{}/\cfAttrChexAttrN{} and \cfAttrChexClinOwned{}/\cfAttrChexClinN{} on CheXpert, and \cfAttrNihAttrOwned{}/\cfAttrNihAttrN{} and \cfAttrNihClinOwned{}/\cfAttrNihClinN{} on NIH.

## app_campaign paragraph 25 — ACCEPTED

**before**

\paragraph{Answer direction: rescue, loss, and templates.} The answer direction $a_q$ of Section~\ref{sec:ansdir} is lifted like the probe normal and written at the same dose against the same reference family, with the other five $a_d$ as its clinical competitors. A concept is \emph{kept} when both $a_q$ and the label direction $\hat w_q$ are owned, \emph{rescued} when only $a_q$ is owned, and \emph{lost} when only $\hat w_q$ is owned (Table~\ref{tab:cf-ansdir}, top); the release lists both ownership contrasts, the column selectivity, and the whitened cosine for every cell. The $a_q$ fitted on the primary template IY is also written unchanged under the rewording WY and the answer-mapped templates IA, IB, WA, and WB, each against its own clean baseline and the $a_d$ of the other five concepts. Each template is graded with the paper's rule: the steering reference against the template's sham, and against the random 95th percentile where the template scores the random family, and the $6\times5$ max-$T$ verdict. A block enters when every held-out template is scored on all 600 test rows (Table~\ref{tab:cf-ansdir}, bottom).

**after**

\paragraph{Answer direction: rescue, loss, and templates.} We lift the answer direction $a_q$ of Section~\ref{sec:ansdir} like the probe normal and write it at the same dose against the same reference family, using the other five $a_d$ as clinical competitors. A concept is \emph{kept} when both $a_q$ and the label direction $\hat w_q$ are owned, \emph{rescued} when only $a_q$ is owned, and \emph{lost} when only $\hat w_q$ is owned (Table~\ref{tab:cf-ansdir}, top). The release reports both ownership contrasts, column selectivity, and the whitened cosine for every cell. We also write the $a_q$ fitted on the primary template IY unchanged under the rewording WY and the answer-mapped templates IA, IB, WA, and WB, each against its own clean baseline and the $a_d$ of the other five concepts. For each template, the paper's grading rule compares the steering reference against the template's sham and, where the template scores the random family, against the random 95th percentile. It also applies the $6\times5$ max-$T$ verdict. A block enters when every held-out template is scored on all 600 test rows (Table~\ref{tab:cf-ansdir}, bottom).

## app_campaign paragraph 26 — ACCEPTED

**before**

\paragraph{Direction and label geometry.} Neither correlations among the six directions nor co-occurrence of the six report labels explains why a competing direction wins (Table~\ref{tab:cf-geometry}). The direction cosine is the raw model-space cosine between two of a block's six write directions, averaged over the off-diagonal pairs of every block; the label $\phi$ is the correlation between two of the six test labels. On NIH, the six directions are as orthogonal as random vectors: their mean off-diagonal cosine is $\cfGeoCosNih$, against a mean absolute cosine of $\cfGeoCosRandom$ between pairs of random unit directions. The labels are nearly uncorrelated (mean $\phi=\cfGeoPhiNih$). Yet NIH shows the same off-diagonal dominance as CheXpert, where directions and labels are correlated (mean off-diagonal cosine $\cfGeoCosChex$; Effusion--Consolidation $\cfGeoCosEffCons$ averaged over blocks, $\phi=\cfGeoPhiEffCons$). The ownership contrast is positive, $O_q>0$, in \cfGeoOwnWinsNihPct\% of the \cfNihCells{} NIH cells and \cfGeoOwnWinsChexPct\% of the \cfChexCells{} CheXpert cells. A cell's strongest competitor is one of five directions, so it coincides with any fixed one of them in one cell in five by chance. It matches the most similar direction at that rate (\cfGeoChanceCosMin{}--\cfGeoChanceCosMax\% of chest cells per dataset), and the label with the largest $\phi$ or the most co-occurring label at that rate too (\cfGeoChanceLabelMin{}--\cfGeoChanceLabelMax\%). Across the \cfGeoOffdiagCells{} off-diagonal chest cells, the competitor's advantage $W_{q,d}-W_{q,q}$ is uncorrelated with the direction cosine (Spearman over cells $\cfGeoRhoAdvCos$, $p=\cfGeoRhoAdvCosP$) and with the label $\phi$ ($\cfGeoRhoAdvPhi$). At least two competitors exceed the own write in \cfGeoTwoAbove{} of \cfGeoTwoAboveN{} chest cells (\cfGeoTwoAbovePct\%), so removing the strongest competitor leaves $O_q$ negative in those cells; the table counts the sign of $O_q$ after that removal, not the owned grade. On COCO, restricting each cell's competitors to a co-occurring family of objects (person with bicycle, $\phi=\cfGeoFamPhiPersonBicycle$; person with dog, $\phi=\cfGeoFamPhiPersonDog$; chair with bottle, $\phi=\cfGeoFamPhiChairBottle$; car with bicycle, $\phi=\cfGeoFamPhiCarBicycle$; person with chair and bottle, mean $\phi=\cfGeoFamPhiPersonChairBottle$) leaves $O_q>0$ in \cfGeoCocoFamMinPct{}--\cfGeoCocoFamMaxPct\% of each family's cells, against \cfGeoCocoFamFullMinPct{}--\cfGeoCocoFamFullMaxPct\% of the same cells with all five competitors. Of the five directions in the seed cell, Nodule is the \emph{least} similar to Effusion (raw model-space cosine $\cfSeedCosNodule$ in this block). It still moves the Effusion answer more than Effusion's own direction ($\cfSeedWNodule$ against $\cfSeedWEff$). The most similar direction, Atelectasis (raw model-space cosine $\cfSeedCosAtel$), moves it by $\cfSeedWAtel$.

**after**

\paragraph{Direction and label geometry.} Neither correlations among the six directions nor co-occurrence of the six report labels explains why a competing direction wins (Table~\ref{tab:cf-geometry}). The direction cosine is the raw model-space cosine between two of a block's six write directions, averaged over the off-diagonal pairs of every block. The label $\phi$ is the correlation between two of the six test labels. On NIH, the six directions are as orthogonal as random vectors: their mean off-diagonal cosine is $\cfGeoCosNih$, against a mean absolute cosine of $\cfGeoCosRandom$ between pairs of random unit directions. The labels are nearly uncorrelated (mean $\phi=\cfGeoPhiNih$). Yet NIH shows the same off-diagonal dominance as CheXpert, where directions and labels are correlated (mean off-diagonal cosine $\cfGeoCosChex$; Effusion--Consolidation $\cfGeoCosEffCons$ averaged over blocks, $\phi=\cfGeoPhiEffCons$). The ownership contrast is positive, $O_q>0$, in \cfGeoOwnWinsNihPct\% of the \cfNihCells{} NIH cells and \cfGeoOwnWinsChexPct\% of the \cfChexCells{} CheXpert cells. Each cell has five competing directions, so its strongest competitor matches any fixed competitor in one cell in five by chance. It matches the most similar direction at this rate (\cfGeoChanceCosMin{}--\cfGeoChanceCosMax\% of chest cells per dataset). It also matches the label with the largest $\phi$ or the most co-occurring label at this rate (\cfGeoChanceLabelMin{}--\cfGeoChanceLabelMax\%). Across the \cfGeoOffdiagCells{} off-diagonal chest cells, the competitor's advantage $W_{q,d}-W_{q,q}$ is uncorrelated with the direction cosine (Spearman over cells $\cfGeoRhoAdvCos$, $p=\cfGeoRhoAdvCosP$) and with the label $\phi$ ($\cfGeoRhoAdvPhi$). At least two competitors exceed the own write in \cfGeoTwoAbove{} of \cfGeoTwoAboveN{} chest cells (\cfGeoTwoAbovePct\%). Removing the strongest competitor therefore leaves $O_q$ negative in those cells. The table counts the sign of $O_q$ after removal, not the owned grade. On COCO, restricting each cell's competitors to a co-occurring family of objects (person with bicycle, $\phi=\cfGeoFamPhiPersonBicycle$; person with dog, $\phi=\cfGeoFamPhiPersonDog$; chair with bottle, $\phi=\cfGeoFamPhiChairBottle$; car with bicycle, $\phi=\cfGeoFamPhiCarBicycle$; person with chair and bottle, mean $\phi=\cfGeoFamPhiPersonChairBottle$) leaves $O_q>0$ in \cfGeoCocoFamMinPct{}--\cfGeoCocoFamMaxPct\% of each family's cells, compared with \cfGeoCocoFamFullMinPct{}--\cfGeoCocoFamFullMaxPct\% of the same cells with all five competitors. Of the five directions in the seed cell, Nodule is the \emph{least} similar to Effusion (raw model-space cosine $\cfSeedCosNodule$ in this block). It still moves the Effusion answer more than Effusion's own direction ($\cfSeedWNodule$ against $\cfSeedWEff$). The most similar direction, Atelectasis (raw model-space cosine $\cfSeedCosAtel$), moves it by $\cfSeedWAtel$.

## app_campaign paragraph 27 — ACCEPTED

**before**

\paragraph{Scale, saturation, refits, and numerics.} The grades hold on the logit scale. Recomputing the grade from the margin $\ell_{\mathrm{yes}}-\ell_{\mathrm{no}}$ reproduces the probability-scale owned grade in \cfScaleAgree{} of \cfScaleAgreeN{} cells. Every owned cell preserves the sign of its ownership contrast on that scale, $O^m_q>0$. The complete owned grade persists in \cfScaleOwnedKeepFull{} of the \cfScaleOwned{} owned cells, with the steering reference recomputed on the margin scale and a positive percentile interval for $O^m_q$ (Table~\ref{tab:cf-scale}). Saturation counters a ceiling explanation: COCO blocks have far more rows at $P(\mathrm{yes})>0.99$ or $<0.01$ than chest blocks (median block share $\cfScaleSatCoco$ against $\cfScaleSatNih$ and $\cfScaleSatChex$). Restricting each cell to its unsaturated rows preserves $O_q>0$ in all \cfScaleUnsatOwned{} owned chest cells and in \cfScaleUnsatOtherPos{} of the \cfScaleUnsatOther{} others. Full regrading of those rows, with the steering reference recomputed on them and the simultaneous max-$T$ advantage over the same six-direction family, preserves the owned grade in \cfScaleUnsatGradeKept{} of \cfScaleUnsatGradeN{} owned chest cells and raises \cfScaleUnsatGradeGained{} of the \cfScaleUnsatGradeOtherN{} others to owned. The same pattern holds in the clean-No, clean-Yes, label-present, and label-absent strata. Refitting the probes on resampled training patients changes $O_q$ by a median of $\cfScaleRefitMedian$. The bf16 batch effects declared at the gates (candidate-logit differences of $\cfScaleBatchMin$--$\cfScaleBatchMax$) affect no owned cell: the margin sign agrees in \cfScaleBatchAgree{} of \cfScaleBatchN{} blocks, and the \cfScaleBatchDisagree{} disagreeing blocks contain no owned cells. Re-scoring the clean baseline in a second module gives bitwise agreement in 100\% of rows for \cfScaleBaselineIdentical{} of \cfScaleBaselineN{} blocks and in at least \cfScaleBaselineRestPct\% for the rest. The difference $|\Delta P(\mathrm{yes})|>0.1$ occurs in at most \cfScaleBaselineDpPct\% of rows. Writes at the connector locus are an order of magnitude smaller than those at the primary locus (median $|W_{q,q}|$ $\cfConnWqqNih$ against $\cfPrimWqqNih$ on NIH and $\cfConnWqqCoco$ against $\cfPrimWqqCoco$ on COCO). The readable and answerable grades take their support from the cohort that carries them, the 400 calibration rows. \cfSupportInelChest{} of the \cfChestAnswerableN{} chest cells have fewer than the ten known positives and ten known negatives that rule requires there: CheXpert Atelectasis, with \cfCalChexInelPos{} known positives and \cfCalChexInelNeg{} known negatives among the 400 calibration rows, in each of the \cfSupportInelChexBlocks{} CheXpert blocks. On NIH every concept clears the rule on that cohort, the smallest count being Pneumothorax at \cfCalNihMinPos{} positives; on COCO the ineligible cell is bicycle, with \cfCalCocoInelPos{} positives, in each of the \cfSupportInelCocoBlocks{} COCO blocks. The test cohort that carries the writes has its own counts (\cfTestChexInelPos{} positives and \cfTestChexInelNeg{} negatives for CheXpert Atelectasis, \cfTestCocoInelPos{} positives for COCO bicycle); they do not enter these two grades. Among eligible cells alone, the shares are \cfSupportReadableChest{} of \cfSupportReadableNChest{} readable (\cfSupportReadablePctChest\%) and \cfSupportAnswerableChest{} of \cfSupportAnswerableNChest{} answerable (\cfSupportAnswerablePctChest\%).

**after**

\paragraph{Scale, saturation, refits, and numerics.} The grades hold on the logit scale. Recomputing the grade from the margin $\ell_{\mathrm{yes}}-\ell_{\mathrm{no}}$ reproduces the probability-scale owned grade in \cfScaleAgree{} of \cfScaleAgreeN{} cells. Every owned cell retains a positive ownership contrast on that scale, $O^m_q>0$. The full owned grade persists in \cfScaleOwnedKeepFull{} of the \cfScaleOwned{} owned cells, with the steering reference recomputed on the margin scale and a positive percentile interval for $O^m_q$ (Table~\ref{tab:cf-scale}). Saturation counters a ceiling explanation: COCO blocks contain far more rows at $P(\mathrm{yes})>0.99$ or $<0.01$ than chest blocks (median block share $\cfScaleSatCoco$ against $\cfScaleSatNih$ and $\cfScaleSatChex$). Restricting each cell to its unsaturated rows preserves $O_q>0$ in all \cfScaleUnsatOwned{} owned chest cells and in \cfScaleUnsatOtherPos{} of the \cfScaleUnsatOther{} others. Full regrading of these rows uses a steering reference recomputed on them and the simultaneous max-$T$ advantage over the same six-direction family. It preserves the owned grade in \cfScaleUnsatGradeKept{} of \cfScaleUnsatGradeN{} owned chest cells and raises \cfScaleUnsatGradeGained{} of the \cfScaleUnsatGradeOtherN{} others to owned. The same pattern holds in the clean-No, clean-Yes, label-present, and label-absent strata. Refitting the probes on resampled training patients changes $O_q$ by a median of $\cfScaleRefitMedian$. The bf16 batch effects declared at the gates (candidate-logit differences of $\cfScaleBatchMin$--$\cfScaleBatchMax$) affect no owned cell. The margin sign agrees in \cfScaleBatchAgree{} of \cfScaleBatchN{} blocks; the \cfScaleBatchDisagree{} disagreeing blocks contain no owned cells. Re-scoring the clean baseline in a second module gives bitwise agreement in 100\% of rows for \cfScaleBaselineIdentical{} of \cfScaleBaselineN{} blocks and in at least \cfScaleBaselineRestPct\% for the rest. Differences satisfying $|\Delta P(\mathrm{yes})|>0.1$ occur in at most \cfScaleBaselineDpPct\% of rows. Writes at the connector locus are an order of magnitude smaller than those at the primary locus (median $|W_{q,q}|$ $\cfConnWqqNih$ against $\cfPrimWqqNih$ on NIH and $\cfConnWqqCoco$ against $\cfPrimWqqCoco$ on COCO). The readable and answerable grades draw their support from the 400 calibration rows. \cfSupportInelChest{} of the \cfChestAnswerableN{} chest cells fail that cohort's requirement of ten known positives and ten known negatives: CheXpert Atelectasis, with \cfCalChexInelPos{} known positives and \cfCalChexInelNeg{} known negatives among the 400 calibration rows, in each of the \cfSupportInelChexBlocks{} CheXpert blocks. Every NIH concept meets the rule on that cohort; Pneumothorax has the smallest count, at \cfCalNihMinPos{} positives. On COCO, bicycle is ineligible, with \cfCalCocoInelPos{} positives, in each of the \cfSupportInelCocoBlocks{} COCO blocks. Support counts for these two grades come solely from calibration; the test cohort carrying the writes has its own counts (\cfTestChexInelPos{} positives and \cfTestChexInelNeg{} negatives for CheXpert Atelectasis, \cfTestCocoInelPos{} positives for COCO bicycle). Among eligible cells, \cfSupportReadableChest{} of \cfSupportReadableNChest{} are readable (\cfSupportReadablePctChest\%) and \cfSupportAnswerableChest{} of \cfSupportAnswerableNChest{} are answerable (\cfSupportAnswerablePctChest\%).

## app_campaign paragraph 28 — ACCEPTED

**before**

\paragraph{The answer direction and the label source.} The answer direction of Section~\ref{sec:ansdir} is fitted to one quantity: the model's own clean answer margin $\ell_{\mathrm{yes}}-\ell_{\mathrm{no}}$ on the block's primary template, read from clean forward passes with the write disabled. Its features are the block's own projected, train-scaled features, its rows the first 3{,}000 rows of the training cohort in the fixed manifest order of that cohort, a seeded shuffle on NIH and a hash of patient or image identity on CheXpert and COCO, into which no label and no answer enters. Per question, a ridge regression of the margin on those features has its penalty chosen by five-fold cross-validated $R^2$ over $\{0.1,1,10,100\}$ and is then refitted on all 3{,}000 rows with the chosen penalty; the folds split rows, which on CheXpert and COCO are one per patient and one per image, and on NIH come from \cfAnsTrainUnitsNih{} patients. The fitted coefficient vector is lifted and normalised exactly as a probe normal is. The radiologist-label cohort enters in two steps, and they test different things. The first re-grades the directions fitted on report-derived labels, unchanged, on \cfValidRows{} films whose labels are a radiologist consensus and whose patients appear in no campaign cohort (Table~\ref{tab:cf-valid}). The second refits the six directions on those radiologist labels, with the block's own projection, its train-only scaler, and the probe settings of the protocol, cross-fitting over patients in \cfValidfitFolds{} folds so that every film is scored by a direction not fitted on it, and writes the refitted directions on the 600 test rows at the primary dose and template against the same random and sham references. Across the \cfValidfitBlocks{} CheXpert blocks that carry it, the expert-label and report-label directions have a median raw model-space cosine of \cfValidfitCosRaw{} and a median covariance-whitened cosine of \cfValidfitCosWhitened{}; the expert-label refit reads the radiologist labels at a median held-out AUROC of \cfValidfitAurocExpert{}, against \cfValidfitAurocReportDir{} for the report-label direction on the same films; and the expert-label directions are owned in \cfValidfitOwnedExpert{} of \cfValidfitCells{} cells, against \cfValidfitOwnedReport{} for the report-label directions of the same blocks.

**after**

\paragraph{The answer direction and the label source.} The answer direction of Section~\ref{sec:ansdir} is fitted to one quantity: the model's own clean answer margin $\ell_{\mathrm{yes}}-\ell_{\mathrm{no}}$ on the block's primary template, read from clean forward passes with the write disabled. Its inputs are the block's own projected, train-scaled features from the first 3{,}000 training-cohort rows in the cohort's fixed manifest order. This order uses a seeded shuffle on NIH and a hash of patient or image identity on CheXpert and COCO; neither labels nor answers enter it. For each question, ridge regression fits the margin from these features, with its penalty chosen by five-fold cross-validated $R^2$ over $\{0.1,1,10,100\}$, then refits on all 3{,}000 rows with the chosen penalty. The folds split rows: one per patient on CheXpert, one per image on COCO, and rows from \cfAnsTrainUnitsNih{} patients on NIH. The fitted coefficient vector is lifted and normalised exactly as a probe normal is. The radiologist-label cohort provides two distinct tests. First, we re-grade the unchanged directions fitted on report-derived labels on \cfValidRows{} films labelled by radiologist consensus. These patients appear in no campaign cohort (Table~\ref{tab:cf-valid}). Second, we refit the six directions on those radiologist labels using the block's own projection, its train-only scaler, and the protocol's probe settings. We cross-fit over patients in \cfValidfitFolds{} folds so that every film is scored by a direction not fitted on it. We write the refitted directions on the 600 test rows at the primary dose and template against the same random and sham references. Across the \cfValidfitBlocks{} CheXpert blocks with this refit, the expert-label and report-label directions have a median raw model-space cosine of \cfValidfitCosRaw{} and a median covariance-whitened cosine of \cfValidfitCosWhitened{}. The expert-label refit reads the radiologist labels at a median held-out AUROC of \cfValidfitAurocExpert{}, against \cfValidfitAurocReportDir{} for the report-label direction on the same films. The expert-label directions are owned in \cfValidfitOwnedExpert{} of \cfValidfitCells{} cells, against \cfValidfitOwnedReport{} for the report-label directions of the same blocks.

## app_campaign paragraph 29 — ACCEPTED

**before**

Table~\ref{tab:cf-geometry} reports direction and label geometry. Table~\ref{tab:cf-scale} reports scale, saturation, refit, connector, and batch statistics supporting Section~\ref{sec:robust-geometry}, and Table~\ref{tab:cf-refit} the full grade under refitted probes. Table~\ref{tab:cf-pairs} reports paired shared-tower differences supporting Section~\ref{sec:reader}. Table~\ref{tab:cf-altdir} reports alternative directions, lifts, token weights, and competitor families. Table~\ref{tab:cf-ansdir} reports the answer direction on the primary and held-out templates, and Table~\ref{tab:cf-attr} non-clinical attribute directions. Table~\ref{tab:cf-precision} reports numerics, Table~\ref{tab:cf-validation} what the answer direction reads and how selective the columns of $W$ are, and Table~\ref{tab:cf-valid} the grade on radiologist labels.

**after**

Table~\ref{tab:cf-geometry} reports direction and label geometry. Table~\ref{tab:cf-scale} reports scale, saturation, refit, connector, and batch statistics supporting Section~\ref{sec:robust-geometry}. Table~\ref{tab:cf-refit} reports the full grade under refitted probes. Table~\ref{tab:cf-pairs} reports paired shared-tower differences supporting Section~\ref{sec:reader}. Table~\ref{tab:cf-altdir} reports alternative directions, lifts, token weights, and competitor families. Table~\ref{tab:cf-ansdir} reports the answer direction on the primary and held-out templates. Table~\ref{tab:cf-attr} reports non-clinical attribute directions. Table~\ref{tab:cf-precision} reports numerics. Table~\ref{tab:cf-validation} reports what the answer direction reads and the selectivity of the columns of $W$. Table~\ref{tab:cf-valid} reports the grade on radiologist labels.

## app_campaign paragraph 32 — ACCEPTED

**before**

Figure~\ref{fig:cf-w-structure} summarises the $6\times6$ write matrices across all completed checkpoints. The top row shows each dataset's cell-wise median matrix. The bottom row links each written direction on the left to the questions it moves on the right. Band width is proportional to the mean positive answer change across checkpoints. Coloured bands reach the writer's own question. Grey bands reach other questions. The write-flow share is the sum of the own-question bands over the sum of all bands: \cfOwnShareNih\% on NIH and \cfOwnShareChex\% on CheXpert, compared with \cfOwnShareCoco\% on COCO. The column-selectivity index $S_d$ of Table~\ref{tab:cf-validation} is a different quantity. It keeps the sign of a direction's own effect, counts negative changes in its denominator, weights every cell equally, and is summarised by the median over cells (\cfValColSelNih{}, \cfValColSelChex{}, and \cfValColSelCoco{}). On the chest sets, the largest sinks are the Cardiomegaly question on NIH and the Edema and Effusion questions on CheXpert. These questions receive writes from every direction.

**after**

Figure~\ref{fig:cf-w-structure} summarises the $6\times6$ write matrices across all completed checkpoints. The top row shows each dataset's cell-wise median matrix. The bottom row links written directions on the left to the questions they move on the right. Band width is proportional to the mean positive answer change across checkpoints. Coloured bands reach the writer's own question. Grey bands reach other questions. The write-flow share is the sum of own-question band widths divided by the sum of all band widths: \cfOwnShareNih\% on NIH and \cfOwnShareChex\% on CheXpert, compared with \cfOwnShareCoco\% on COCO. The column-selectivity index $S_d$ in Table~\ref{tab:cf-validation} measures a different quantity. It retains the sign of a direction's own effect. Its denominator includes negative changes. It weights every cell equally. We report the median over cells (\cfValColSelNih{}, \cfValColSelChex{}, and \cfValColSelCoco{}). On the chest sets, the largest sinks are the Cardiomegaly question on NIH and the Edema and Effusion questions on CheXpert. These questions receive writes from every direction.

## app_campaign paragraph 35 — ACCEPTED

**before**

Figure~\ref{fig:cf-examples} shows grades on individual test images. For a chest example, the pool contains test rows where the finding is labelled present, the probe reads it (probability above $0.5$), and the clean answer is No. For a natural-image example, the pool contains rows where the probe reads the label correctly and the clean answer is No. We split each pool by whether the concept's own write or the strongest competing write raises the answer more. We show the row whose gap is closest to the median among rows where either write raises the answer by at least $0.2$. Each example's last line reports the number of pool rows with the same ordering, giving that example its own base rate.

**after**

Figure~\ref{fig:cf-examples} shows grades for individual test images. For a chest example, the pool contains test rows where the finding is labelled present, the probe reads it (probability above $0.5$), and the clean answer is No. For a natural-image example, the pool contains rows where the probe reads the label correctly and the clean answer is No. We split each pool by which write raises the answer more: the concept's own write or the strongest competing write. Among rows where either write raises the answer by at least $0.2$, we show the row whose gap is closest to the median. Each example's last line reports how many pool rows share its ordering, giving that example its own base rate.

## app_repro paragraph 1 — ACCEPTED

**before**

This appendix specifies the consumed block of every family, the consumed-token mask, the projection and probe, the control constructions, the cohort roles, and the protocol record at the level of the registered protocol; the released code implements each of them.

**after**

This appendix specifies each family's consumed block, the consumed-token mask, the projection and probe, the control constructions, the cohort roles, and the protocol record at the registered protocol's level of detail. The released code implements each component.

## app_repro paragraph 3 — ACCEPTED

**before**

The primary locus is the output of the last visual block whose output the consumer reads, and the connector locus is the consumer's output that enters the language model. The block is identified from each checkpoint's own configuration when the checkpoint is loaded, and each block's preflight record stores the identified block, its hidden dimension, its consumer, and any branch that bypasses it.

**after**

The primary locus is the output of the last visual block that feeds the consumer. The connector locus is the consumer's output that enters the language model. We identify the block from each checkpoint's own configuration when loading the checkpoint. Each block's preflight record stores the identified block, its hidden dimension, its consumer, and any branch that bypasses it.

## app_repro paragraph 6 — ACCEPTED

**before**

The write of Equation~\ref{eq:intervention} changes only the consumed tokens, the token positions the consumer reads at the locus. They are derived for each batch from the processor's outputs for that batch:

**after**

The write in Equation~\ref{eq:intervention} changes only the consumed tokens at the positions the consumer reads at the locus. We derive these positions for each batch from that batch's processor outputs:

## app_repro paragraph 8 — ACCEPTED

**before**

Scoring stops with an error, rather than falling back, when an image span does not exactly cover the locus, a mask length differs from the number of tokens at the locus, an image has no consumed token, or the locus is reached more than once in one forward pass.

**after**

Scoring stops with an error and no fallback when an image span does not exactly cover the locus, a mask length differs from the token count at the locus, an image has no consumed token, or one forward pass reaches the locus more than once.

## app_repro paragraph 9 — ACCEPTED

**before**

Preflight check (C) verifies the mask. It captures the locus tensor on a clean pass and a pass steered at $\alpha=+0.25$ along the first concept direction. It reports the largest absolute difference outside the mask and the largest absolute changes in the consumer's output and answer logits. Across all \cfBlocks{} blocks, the change outside the consumed tokens is $0$ at both loci, while both the consumer's output and the answer logits change. The same records show a bitwise-identical repeated clean pass (check A) and a bitwise-identical $\alpha=0$ pass (check B) for every block.

**after**

Preflight check (C) verifies the mask. It captures the locus tensor on a clean pass and a pass steered at $\alpha=+0.25$ along the first concept direction. It reports the maximum absolute difference outside the mask and the maximum absolute changes in the consumer's output and answer logits. Across all \cfBlocks{} blocks, the change outside the consumed tokens is $0$ at both loci. The consumer's output and answer logits both change. The same records show a bitwise-identical repeated clean pass (check A) and a bitwise-identical $\alpha=0$ pass (check B) for every block.

## app_repro paragraph 11 — ACCEPTED

**before**

The projection $R\in\R^{D\times512}$ is one draw of standard normal variates from a PCG64 generator seeded with $0$, divided by $\sqrt{512}$ and stored in float32, so its entries are independent $\mathcal{N}(0,1/512)$ variates. The seed is $0$ for every checkpoint, dataset, and locus; $D$ is the family's hidden dimension. We neither normalise nor orthogonalise the columns of $R$. We normalise the lifted direction of Equation~\ref{eq:lift} once, in model space, to unit $\ell_2$ norm. The Gram matrix $R^{\top}R/D$ has full rank 512 in every one of the \cfAltdirdBlocks{} blocks that carry the displacement module, with eigenvalues $\cfAltdirdEigMin$--$\cfAltdirdEigMax$ and condition number \cfAltdirdCondMin{}--\cfAltdirdCondMax{}.

**after**

We construct the projection $R\in\R^{D\times512}$ by drawing standard normal variates once from a PCG64 generator seeded with $0$, dividing by $\sqrt{512}$, and storing the result in float32. Its entries are independent $\mathcal{N}(0,1/512)$ variates. The seed is $0$ for every checkpoint, dataset, and locus. $D$ is the family's hidden dimension. We neither normalise nor orthogonalise the columns of $R$. We normalise the lifted direction of Equation~\ref{eq:lift} once to unit $\ell_2$ norm in model space. The Gram matrix $R^{\top}R/D$ has full rank 512 in all \cfAltdirdBlocks{} blocks that carry the displacement module, with eigenvalues $\cfAltdirdEigMin$--$\cfAltdirdEigMax$ and condition number \cfAltdirdCondMin{}--\cfAltdirdCondMax{}.

## app_repro paragraph 12 — ACCEPTED

**before**

The token-mean read vector $\hvec_i$ yields projected features $R^{\top}\hvec_i$. A featurewise standardiser fitted only on training rows supplies the mean $\boldsymbol\mu$ and the scale $\boldsymbol s$, and every row is standardised as $(R^{\top}\hvec-\boldsymbol\mu)\oslash\max(\boldsymbol s,10^{-8})$. Each concept probe is an $\ell_2$-regularised logistic regression with inverse regularisation strength $C=1$, fitted by L-BFGS for at most 2{,}000 iterations with unweighted classes and seed $0$ on the training rows with a known label for that concept. The positive-class coefficient fixes the clinical pole of the direction. For each of the two refit seeds, we resample training units with replacement, retain every row of each sampled unit at that unit's multiplicity, and refit the scaler and six probes using the same projection. The random family, shams, and control probes remain at seed 0.

**after**

Projecting the token-mean read vector $\hvec_i$ gives features $R^{\top}\hvec_i$. A featurewise standardiser fitted only on training rows supplies the mean $\boldsymbol\mu$ and scale $\boldsymbol s$. Every row is standardised as $(R^{\top}\hvec-\boldsymbol\mu)\oslash\max(\boldsymbol s,10^{-8})$. Each concept probe is an $\ell_2$-regularised logistic regression with inverse regularisation strength $C=1$. We fit each probe by L-BFGS for at most 2{,}000 iterations with unweighted classes and seed $0$, using training rows with a known label for that concept. The positive-class coefficient fixes the direction's clinical pole. For each of the two refit seeds, we resample training units with replacement, retaining all rows of each sampled unit at its multiplicity. We then refit the scaler and six probes using the same projection. The random family, shams, and control probes remain at seed 0.

## app_repro paragraph 14 — ACCEPTED

**before**

\paragraph{Random directions.} We draw one $119\times D$ standard-normal matrix from a PCG64 generator seeded with $0$ and normalise each row to unit $\ell_2$ norm. The same 119 directions serve every question in the block.

**after**

\paragraph{Random directions.} We draw one $119\times D$ standard-normal matrix using a PCG64 generator seeded with $0$ and normalise each row to unit $\ell_2$ norm. Every question in the block uses the same 119 directions.

## app_repro paragraph 15 — ACCEPTED

**before**

\paragraph{Coordinate-permutation sham.} Immediately after the random draw, the same generator produces one permutation of $\{0,\dots,D{-}1\}$ per concept in protocol concept order. Concept $c$'s sham is $\hat\wvec_c$ with its coordinates rearranged by concept $c$'s own permutation. The sham preserves the direction's coordinate multiset and unit norm. Each question is written with its own sham.

**after**

\paragraph{Coordinate-permutation sham.} Immediately after the random draw, the same generator produces one permutation of $\{0,\dots,D{-}1\}$ per concept, in protocol concept order. We form concept $c$'s sham by rearranging the coordinates of $\hat\wvec_c$ with concept $c$'s own permutation. This preserves the direction's coordinate multiset and unit norm. We write each question with its own sham.

## app_repro paragraph 16 — ACCEPTED

**before**

\paragraph{Matched random-label probes.} Each row has a type: view $\times$ sex $\times$ age decade on the chest sets, and aspect-ratio $\times$ area buckets on COCO. For control seed $k\in\{0,\dots,19\}$, we traverse the types in sorted order and draw one label per type, uniformly from $\{0,1\}$, from a PCG64 generator seeded with $k$; if all types receive the same label, we flip the first. Every row of a type receives that type's label. We fit each control probe on the same training rows and known-label subset, with the same hyperparameters as the concept probe, giving it the concept probe's capacity and nuisance structure. A block fits control probes when its training rows span at least eight types. Selectivity is Equation~\ref{eq:selectivity}. Its bootstrap over calibration units keeps a draw when the real AUROC is finite and at least ten of the twenty control AUROCs are finite. We grade a cell for readability when at least 1{,}900 of the 2{,}000 draws are kept.

**after**

\paragraph{Matched random-label probes.} Each row has a type: view $\times$ sex $\times$ age decade on the chest sets, and aspect-ratio $\times$ area buckets on COCO. For control seed $k\in\{0,\dots,19\}$, we traverse the types in sorted order and use a PCG64 generator seeded with $k$ to draw one label per type uniformly from $\{0,1\}$. If all types receive the same label, we flip the first. Every row of a type receives that type's label. We give each control probe the concept probe's capacity and nuisance structure by fitting it on the same training rows and known-label subset with the same hyperparameters. A block fits control probes when its training rows span at least eight types. Selectivity is Equation~\ref{eq:selectivity}. Its bootstrap over calibration units retains a draw when the real AUROC is finite and at least ten of the twenty control AUROCs are finite. We grade a cell for readability when at least 1{,}900 of the 2{,}000 draws are retained.

## app_repro paragraph 18 — ACCEPTED

**before**

Table~\ref{tab:cf-roles} lists each role's size and use. The roles are disjoint by patient on the chest sets and by image on COCO. We use the calibration role only to determine readability and answer capability. We fit no probe and score no write on it. The test role carries every write.

**after**

Table~\ref{tab:cf-roles} lists each role's size and use. The roles are disjoint by patient on the chest sets and by image on COCO. We use the calibration role only to determine readability and answer capability. We neither fit probes nor score writes on it. Every write uses the test role.

## app_repro paragraph 21 — ACCEPTED

**before**

Each block writes a run record containing the protocol identifier \texttt{cf-transfer-v1}, a run identifier that names the checkpoint and the dataset, the version of the evaluation code that produced the block, the pinned checkpoint and processor revisions, the library versions, the numeric precisions, and the resolved processing settings, including the rendered chat template. All \cfBlocks{} blocks carry the protocol identifier \texttt{cf-transfer-v1}, and the protocol file registers the code version it was frozen against.

**after**

Each block writes a run record containing the protocol identifier \texttt{cf-transfer-v1}, a run identifier naming the checkpoint and dataset, the evaluation code version that produced the block, the pinned checkpoint and processor revisions, the library versions, the numeric precisions, and the resolved processing settings, including the rendered chat template. All \cfBlocks{} blocks carry the protocol identifier \texttt{cf-transfer-v1}. The protocol file registers the code version it was frozen against.

## app_repro paragraph 22 — ACCEPTED

**before**

Table~\ref{tab:cf-amendments} lists additions to the protocol file after the first wave and the date of each addition. The six planned modules and the connector calibration derived from them carry no amendment date. The robustness tables in Appendix~\ref{app:cf-robustness} identify the blocks that carry each amendment module.

**after**

Table~\ref{tab:cf-amendments} lists each addition to the protocol file after the first wave and its date. The six planned modules and the connector calibration derived from them have no amendment date. The robustness tables in Appendix~\ref{app:cf-robustness} identify the blocks containing each amendment module.


{"sent": 55, "accepted": 54, "rejected": 1}
